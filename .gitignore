# macOS
.DS_Store

# Git worktrees
.worktrees/

# Node dependencies (monorepo root — pnpm hoists shared deps here)
node_modules/
.pnpm-store/
*.tsbuildinfo
.turbo/

# MCP secrets
.mcp.local.json

# Claude Code local settings (may contain secrets — never commit)
.claude/settings.local.json

# direnv (project-scoped env vars — Supabase MCP token lives here)
.envrc

# dotenv
.env
.env.local

# Runtime artifacts
.claude/scheduled_tasks.lock

# Supabase CLI link cache
apps/web/supabase/.temp/

# UI-excellence audit screenshots (heavy, kept local in ceo worktree)
docs/design/ui-excellence-audit/screenshots/
docs/design/ui-excellence-audit/screenshots-after/
docs/design/ui-excellence-audit/screenshots-final/
