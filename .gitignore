# macOS
.DS_Store

# Git worktrees
.worktrees/

# MCP secrets (shouldn't be needed since .mcp.json uses env var expansion,
# but belt-and-suspenders)
.mcp.local.json

# direnv (project-scoped env vars — Supabase MCP token lives here)
.envrc

# dotenv
.env
.env.local
