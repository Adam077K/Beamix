---
date: 2026-05-29
agent: cmo
task: W2.2 weekly digest — Resend template + section reference doc
qa_verdict: PASS
tier: full
team: beamix-w2.2-digest
---

## Surface
Resend email template: `apps/web/src/lib/email/templates/weekly-digest.tsx`
Section reference doc: `docs/05-marketing/weekly-digest-section-reference.md`

## Customer language used
- "Your team" framing throughout (done-for-you voice)
- "Pushed schema markup to…", "Submitted a citation to…" (action-verb first, specific platforms)
- Footer close: "Questions about your digest? Reply to this email." (warm, direct)
- Section 2 edge case: "Your team is building your foundation this week. First deliverables land next week."

## CTO coordination
Agreed on: (1) structured JSON output shape, (2) 5-section list, (3) WeeklyDigestProps schema.
CTO additions accepted: `digestId` (future view-in-browser), `customerTier` (future tier-gating).
Pilot caveats agreed: placeholder approval URLs (W2.3 scope), mock data fallback, UTC timing only.

## Key decisions
- **Output shape:** Structured JSON from AI digest-writer agent → Resend template renders. Decoupled.
- **Section order:** Visibility delta → Wins → Approvals (conditional) → Next week → Footer close.
- **Template reuse:** HYBRID — kept styles object from existing `feat/be-w2-weekly-digest` file (brand-compliant), rebuilt component from locked WeeklyDigestProps spec. Existing schema diverged too deeply (9 missing props, wrong section order, signedToken vs approveUrl approach).
- **Single card layout:** One card containing all sections (cleaner than multi-card for email).

## Voice canon check: PASS
- Zero banned words, zero emojis, zero AI labels
- CTA copy locked: "Approve →", "Approve all N items →", "See all N wins →"
- Section headers Sentence case throughout
- Score colors data viz only (badge + delta indicator), never on buttons/links
- Footer close hardcoded, not a prop

## Pre-merge actions (QA gate — not yet complete)
1. File at main repo root (untracked). Must be committed on `feat/be-w2-weekly-digest` branch.
2. Run `pnpm install && pnpm typecheck` in `apps/web/` — node_modules missing prevented tsc in this session.
