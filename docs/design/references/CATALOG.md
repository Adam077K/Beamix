# Reference Catalog — sorted 2026-06-05

40 references sorted from the founder's drop (37) + 3 consolidated seeds. This is the master contract: every image's **source · kind · the move to Take · what to Leave (color/brand always → Beamix white+blue) · maps-to · observed swatches**. The `product-designer` and `design-critic` read this. **The law:** color, fonts, brand are never taken from a reference — only the named move. Dark references become white-and-blue Beamix.

Legend — cluster tags: `warm-editorial` · `pastel-gradient` · `dense-dashboard` · `colorful-charts` · `floating-bento` · `calm-dense-list` · `character` · `cinematic-3d` · `split-onboarding` · `nav-shell` · `ai-diagram`.

---

## `_product-feel/` — the global soul (LOCKED 2026-06-05, founder checkpoint #1)

> Locked at 6. Founder confirmed the proposed set and moved the earlier `feel-stripe-demoted.png` seed out to `home/`. This set loads on every screen build.

- **feel-posthog-product.png** — PostHog product UI (already mocked as "Beamix"). *Take: the white-canvas product-quality bar — light sidebar with subtly colored nav icons, bordered rounded dashboard cards, calm density, designed empty states with a character.* Leave: nothing to recolor (already white+restrained); just rebrand to #3370FF. Maps: the whole product. Swatches: `#FFFFFF #FBFBFA #EAEAEA #3370FF #1A1A1A`. Cluster: dense-dashboard. **NORTH-STAR.**
- **feel-anthropic-editorial.png** — Anthropic homepage. *Take: warm editorial confidence — one huge grotesque headline with underline accents + a serif display moment (Fraunces), generous calm.* Leave: the cream #F4F1EA canvas → Beamix white/off-white; near-black ink kept. Maps: home, hero, type system. Swatches: `#F4F1EA #16140F #E8E4DA #5B6470`. Cluster: warm-editorial. **NORTH-STAR (type/soul).**
- **feel-dia-blue-hero.png** — Dia (Browser Company) hero. *Take: mixed serif/sans headline over a soft #3370FF→white radial arc + corner floating pill-cards + top floating pill nav.* Leave: almost nothing — its blue gradient IS Beamix blue. Maps: home, hero, empty-hero. Swatches: `#3F6FE0 #7C9CEC #EAF0FB #16140F`. Cluster: pastel-gradient. **NORTH-STAR (closest to Beamix already).**
- **feel-attio-whitespace.png** — Attio homepage. *Take: editorial discipline — hairline section dividers, generous whitespace rhythm, small honest screenshots, color as rare punctuation not fill.* Leave: its multicolor dots → let #3370FF be the one accent. Maps: home, overall spacing/layout system, footer. Swatches: `#FFFFFF #F6F6F7 #1C1C1E #8A8A8E #3B6FE0`. Cluster: warm-editorial.
- **feel-raycast-quality.png** — Raycast site. *Take: bento card composition, generous spacing, mono technical labels (FIG.01), electric-blue 3D line-art illustration.* Leave: dark #0A0A0B canvas → white; keep the blue illustration accent. Maps: home, feature sections, illustration style. Swatches: `#0A0A0B #3B6FF2 #1C2230 #E4E4E7`. Cluster: floating-bento.
- **feel-linear.png** — Linear (CEO seed, kept). *Take: quiet confidence, dense data that breathes, considered hierarchy, zero decoration.* Leave: dark palette → white. Maps: whole product. Swatches: `#0B0B0E #E4E4E7 #5B6B86 #3B7BF7`. Cluster: calm-dense-list. **NORTH-STAR.**

---

## `home/` — product home + hero (screen board)

- **posthog-product-home.png** — PostHog "Hello Adam" home. *Take: a centered command bar ("What can I help you with?") over Pinned/Recents/Starred columns — calm product home with one input as the hero.* Leave: keep light; #3370FF on the input focus + links. Maps: dashboard home, command palette. Swatches: `#FFFFFF #FAFAFA #6B7280 #3370FF`. Cluster: dense-dashboard.
- **mixpanel-home-hero.png** — Mixpanel hero. *Take: centered huge headline over a soft wash + a single row of outlined capability chips + a bottom-bleeding product screenshot for depth.* Leave: lavender wash → white + #3370FF chips. Maps: home hero, onboarding intro. Swatches: `#FBFAFF #EDE7F6 #1A1333 #5B4B8A`. Cluster: editorial-home.
- **retool-hero-3d.png** — Retool hero. *Take: cinematic full-bleed 3D refraction render grounded by a quiet monochrome logo wall underneath.* Leave: dark moody palette → light-mode 3D in #3370FF-family on near-white. Maps: dashboard/empty hero, "works with ChatGPT/Gemini/Perplexity" engine logo row. Swatches: `#0C0C0F #2E6E7E #B5764C #6FB6C4`. Cluster: cinematic-3d.
- **retool-hero-headline-iso.png** — Retool headline + iso bento. *Take: oversized two-line display headline + muted subhead, paired with isometric "building block" renders that explain without a screenshot.* Leave: teal/copper film → Beamix light-mode iso (#3370FF + slate). Maps: section headers, "how it works" iso scenes. Swatches: `#0A0A0D #7FA8A0 #4E6E8E #E7E4DF`. Cluster: cinematic-3d.
- **anthropic-editorial-block.png** — Anthropic "Project Glasswing". *Take: left serif headline + right organic Voronoi/cellular artwork — one type moment + one textured asset. The cellular mesh maps perfectly to a "search graph / how the scan sees the web" motif.* Leave: near-black canvas → off-white; recolor mesh to #3370FF-tint on white. Maps: home, hero, scan-graph visual. Swatches: `#16140F #F4F1EA #5B6470`. Cluster: warm-editorial.
- **superhuman-home-color-1..5.png** — Superhuman homepage (5 frames). *Take: floating layered product cards with dotted connector lines + soft shadows, colored category tags, a tabbed product switcher with colored app icons. PRIMARY COLOR SOURCE.* Leave: keep the technique; recolor washes to Beamix gradient family. Maps: home, dashboard cards, status tags, the whole palette expansion. Swatches: `#F2F2F0 #6B5BFF #F4C8D8 #BFD4F0 #2E4A4A #5A2A2A`. Cluster: pastel-gradient.

---

## `dashboard/` — dashboard screen board

- **north-star-plausible.png** — Plausible live demo (real dashboard). *Take: KPI tile-strip → one dominant chart → dense-calm tables; primary KPI is the hero.* Leave: indigo palette + flat edges → #3370FF + layered shadow. Maps: dashboard. Swatches: `#FFFFFF #6366F1 #F5F5F5 #111827`. Cluster: dense-dashboard.
- **posthog-empty-dashboard.png** — PostHog empty dashboard. *Take: a real dashboard in its EMPTY state — titled bordered cards (DAILY ACTIVE USERS + info icon + "…"), skeleton glyph inside, "no matching events" copy, a retention heatmap with subtle tinted cells. Empty states designed, not blank.* Leave: keep light; heatmap tints → blue ramp. Maps: dashboard empty/first-run, score cards, retention/visibility grid. Swatches: `#FFFFFF #F7F7F8 #E8E8EA #6B7280 #C7B8F0`. Cluster: dense-dashboard. **NORTH-STAR (empty-state quality).**
- **dense-kpi-ribbon.webp** — Plausible/Coda-style analytics. *Take: a black KPI ribbon with 6 oversized numbers over tiny captions (extreme number-over-label hierarchy), a two-band area chart with a dotted forecast tail, and tables with in-cell horizontal bar shading.* Leave: indigo fills → #3370FF + pale-blue tint; keep the black ribbon as the one dark anchor on white. Maps: dashboard score cards, scan trend chart, rankings table. Swatches: `#FFFFFF #111111 #4B3FD4 #C9C2F0 #6B6B6B`. Cluster: dense-dashboard.

---

## `onboarding/` — onboarding screen board (new)

- **coda-onboarding-survey.png** — Coda onboarding (mislabeled "Superhuman"). *Take: split screen — left panel with segmented step pills + bold question + radio options + forward CTA; right side a spotlit product screenshot you're "walking toward."* Leave: plum/violet dark stage → off-white or one deep slab + soft #F4F6FF glow; #3370FF radios. Maps: onboarding, setup wizard, business-profile capture. Swatches: `#1E1B24 #6B4A8F #F5C84B #FFFFFF`. Cluster: split-onboarding.
- **coda-onboarding-welcome.png** — Coda onboarding welcome. *Take: personalized first-name welcome + 3-bullet value list + single "Get started →"; right screenshot advances to show progress.* Leave: amber keyword highlight → #3370FF. Maps: onboarding welcome, post-signup activation. Swatches: `#1C1A22 #E5A93C #FFFFFF #9A8FA8`. Cluster: split-onboarding.

---

## `_components/cards/`

- **raycast-cards-spacing.png** — Raycast feature cards. *Take: bordered cards on dark with generous internal padding + an inline search/command result list inside one card; spacing rhythm is the lesson.* Leave: dark → white, 1px border + soft shadow, #3370FF accents. Maps: dashboard cards, paywall, settings cards. Cluster: floating-bento.
- **raycast-bento-dark.png** / **raycast-bento-white.png** — Raycast bento (founder noted "but white"). *Take: bento grid of feature cards with blue 3D illustrations + mono labels; the white variant proves it works on light.* Leave: keep white variant's direction; #3370FF illustrations. Maps: home/feature bento, dashboard cards. Cluster: floating-bento.
- **retool-feature-triad.png** — Retool Build/Launch/Scale. *Take: 3 equal cards each with a centered concentric radar/aperture dot-grid that lights up differently per stage (progression narrated by the diagram); kicker→title→body→arrow-link anatomy.* Leave: multi-hue stages → slate dot-grid + #3370FF active; amber/green only if a real state taxonomy needs them. Maps: dashboard diagnosis cards, scan-stage cards, GEO-score radar. Cluster: floating-bento.
- **retool-bento-proof.png** — Retool proof bento. *Take: masonry of mixed-size tiles — big stat-led copy tiles interleaved with photo tiles + one deep-navy CTA tile; stat-first typography keeps each scannable.* Leave: dark mosaic → white/#F7F9FC tiles, one #3370FF focal tile. Maps: results/impact tiles, "your GEO wins" bento, report highlights. Cluster: floating-bento.
- **anthropic-editorial-cards.png** — Anthropic latest-releases cards. *Take: flat cards with serif headline + labeled DATE/CATEGORY metadata grid (hairline rules) + pill "Read →"; zero shadow.* Leave: cream → off-white #FAFAF8 + faint border; tags/arrows #3370FF. Maps: home latest-scans/report cards, changelog, blog. Cluster: warm-editorial.
- **mixpanel-split-feature-card.png** — Mixpanel feature split. *Take: one oversized realistic UI mock (with a mini floating toolbar pinned to its bottom edge) paired against a typographic check-listed benefit column.* Leave: pink/lavender tint → white card + #3370FF checks/toolbar. Maps: home feature sections, scan-result explainer cards. Cluster: editorial-home.

## `_components/charts-dataviz/`

- **mixpanel-charts-collage.png** — Mixpanel dashboard collage. *Take: chart cards with per-bar % labels above each bar, pastel multi-band area fills, an activation bar set, a left-docked AI chat rail, and a floating modal lifted on a stronger shadow over the grid.* Leave: all-purple series → restrained Beamix ramp anchored on #3370FF + 2–3 desaturated tints; single resting-shadow tier, heavier only for the floating overlay. Maps: scan charts, visibility trend charts, score cards, agent-chat rail. Cluster: colorful-charts. **(palette source).**

## `_components/tables-lists/`

- **linear-agent-run-log.png** — Linear issue list (reads like an agent-run log). *Take: ultra-dense single-line rows — drag dots → mono ID (ADA-21) → status icon → title → right-pinned label pill + avatar + relative time; collapsible group headers with inline counts; 12+ rows per viewport, no card chrome.* Leave: dark rows → white + #F4F6FA hover; status pills slate-on-white, #3370FF reserved for active/running. Maps: agent-run log, scan history, recommendations queue. Cluster: calm-dense-list. **(directly relevant to Beamix agent runs).**

## `_components/empty-first-run/`

- **linear-projects-empty.png** — Linear projects empty. *Take: feature-glyph illustration + two-tier CTA (primary pill showing the `N` keyboard shortcut + quiet secondary doc link) — teaches action and keyboard model at once.* Leave: dark + warm/cool glyph → white, monochrome line glyph, #3370FF primary pill, soft #EEF2FF shortcut chip. Maps: empty states for Scans/Agent Runs/Reports. Cluster: character/empty.
- **posthog-empty-character.png** — PostHog "Create your first annotation". *Take: a friendly mascot character (hedgehog) holding something next to the primary CTA + an "or do it from your agent" secondary path — premium, warm empty state with personality.* Leave: keep light; character recolored to Beamix. Maps: the Beamie character + every empty state. Swatches: `#FFFFFF #6B7280 #3370FF`. Cluster: character. **NORTH-STAR (character direction).**
- **ship-pixel-mascots.png** — Ship conference site. *Take: a constellation of tiny distinct pixel-art mascots scattered through negative space + a lo-fi pixel display headline + a hand-drawn wobble-outline button — personality filling empty space without heavy illustration.* Leave: black → off-white; mascots in near-black line + #3370FF fills; one pixel headline as a controlled accent only. Maps: Beamie character, empty/zero-data, 404, loading personality. Cluster: character.

## `_components/nav-shell/`

- **coda-doc-shell.png** — Coda doc app shell. *Take: slim left sidebar (workspace switcher top, emoji-prefixed page tree, a clearly highlighted active row, "+ New page", user pill bottom) beside a doc canvas using emoji-flag callout blocks + inset info-card panels for first-run guidance.* Leave: dark → white/off-white shell, near-white sidebar, #3370FF active-row + callout accents. Maps: dashboard sidebar, settings nav, first-run guidance. Cluster: nav-shell.

## `_components/forms-inputs/`

- **linear-workspace-settings.png** — Linear workspace settings. *Take: left grouped-nav rail + centered single-column form in labeled sections (Logo/Name/URL/Region/Danger zone), each field label-left/control-right in a hairline card; a dedicated red-text "Danger zone" — color purely as semantic signal.* Leave: dark → white app shell, #FAFBFC panel, hairline dividers, #3370FF active nav, red kept only for destructive. Maps: Settings (Workspace/Members/Billing/Integrations), profile forms, danger-zone flows. Cluster: neutral-settings.

## `_components/overlays-detail/`

- **mixpanel-ai-engine-diagram.png** — Mixpanel AI band. *Take: logo tiles (ChatGPT/Gemini/Notion…) at top connected by thin curved hairline traces converging down into a single glowing orb-node — literally "all engines feed one brain." Maps perfectly to Beamix's multi-LLM scan → one verdict.* Leave: maroon/oxblood band → deep near-black/navy OR a white panel with #3370FF traces + blue-glow orb. Maps: scan engine diagram, AI/agents section, "how it works". Swatches: `#3D1626 #C0563E #E8C4B0`. Cluster: ai-diagram. **(strong Beamix-specific fit).**

## `_components/motion/`

- **dia-card-morph-1/2/3.png** — Dia "reads between the tabs" (scroll-synced sequence). *Take: a pinned numbered left index (01/02/03) where the active item is bold and inactives dim to gray, while ONE right-hand product card cross-fades through three states — incl. a chat thread showing live "Searched Slack / email / Notion" tool-call steps. Reuse that live-tool-call-step pattern verbatim for Beamix scan/agent progress.* Leave: warm cream/butter-yellow art → white + near-black ink, #3370FF active-step + tool-call chips, #9CA3AF inactive. Maps: home feature section, scan-in-progress live steps, agent tool-call feed. Cluster: animated-cards. **(directly relevant to scan/agent progress UI).**
