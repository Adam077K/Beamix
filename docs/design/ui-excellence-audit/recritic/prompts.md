# Re-critic — /prompts (PASS gate)

**Verdict: PASS** (with one P2 polish note + a capture-coverage caveat)

Graded the polished `populated-desktop.png` against the Profound/Otterly competitor bar and the CRAFT-SYSTEM 8-tells / 12-moves rubric. Only `populated-desktop.png` exists in `screenshots-final/prompts/` — no mobile or empty capture, so mobile and the designed-empty state (M8) could not be verified visually.

## What's strong (the bar is met on desktop)
- **TIER-1 focal is clear (M1/M10).** The hero "Prompt Explorer" card carries the single 64px mono `10` figure with its own micro-sparkline and "TRACKED PROMPTS" eyebrow. One focal above the fold; nothing competes.
- **4-step type contract is visibly stepped (M2).** Eyebrow `BRIGHT SMILE DENTAL` (12px uppercase muted) → display "Prompt Explorer" → body sub → 64px mono figure. Obvious gaps between registers.
- **Fraunces beat, exactly one (M5).** "Your query landscape is *growing*." — italic serif on the verdict word only, inline in a sans sentence, not in chrome.
- **Signature detail present (M4/M11).** Mono micro-sparklines in the FREQUENCY column (+12 / +10 / +14) and under the hero figure. Pos (#4/#6/#1) and Frequency (54/47/38) read mono tabular.
- **Earned asymmetry (M3).** Dominant left column (hero + query-landscape table) + narrower right rail (Coverage 3/10, Top Gaps to Close). No N-equal grid.
- **Blue=you / violet=agents holds (M6).** Engine tags use blue (ChatGPT/Gemini) + violet (Perplexity); "Let Beamix handle it" agent affordance sits beside "Run it myself". Violet appears on tags/agent zone, never on a button.
- **Depth staging felt (M1).** Hero raised, table standard, right-rail cards recede. Reads layered, not uniform.
- Sits credibly beside Profound's prompt/citation tables and Otterly's ranking views as one hand.

## Remaining findings (do not block PASS)
- **P2 — dual blue primary in the action band.** "Run it myself" (blue fill) and "Run Query Mapper" (blue fill) are both saturated-blue CTAs in close proximity, softening the single-primary discipline. Fix: keep ONE blue fill as the primary action and demote the other to a secondary/outline or `#0A0A0A` treatment so the eye has one true primary per surface.
- **Caveat (capture, not craft):** only `populated-desktop.png` was captured. Mobile responsive behavior and the designed-empty state (M8 two-tier recovery) are unverified. Recommend capturing `populated-mobile` + `empty-desktop` before final sign-off, but the desktop polished state meets the bar.

No AI tells dominate. No bug. Does not read AI-generated.
