# Re-critic — /ask (RE-CRITIC PASS gate)

**Verdict: PASS**
**Graded:** `screenshots-final/ask/populated-desktop.png` (1440×900) vs Profound + Otterly answer-engine refs + CRAFT-SYSTEM 8 tells / 12 moves.
**State coverage:** populated-desktop ONLY. No empty-desktop or populated-mobile capture present — see P2.

## Why this passes
The polished /ask render is genuinely excellent and reads as one hand beside Profound/Otterly — arguably ahead of them on editorial craft because it has a real point of view (a cited-first verdict + visible provenance), not a generic prompt list.

All 8 AI tells absent in the rendered state:
- **One TIER-1 focal (M1/M10):** the big mono `5 /6 tests` answer figure is the single dominant focal; the `GROUNDED IN` card recedes on a violet-tint ground. No competing hero card.
- **4-step type contract visibly stepped (M2):** STEP-1 mono `5` figure · STEP-2 30px display heading "Ask anything about your visibility" · STEP-3 12px uppercase gray eyebrows ("ASK BEAMIX", "CITED FIRST · CHATGPT + GEMINI", "GROUNDED IN") · STEP-4 13–15px prose. Gaps are obvious.
- **Earned asymmetry (M3):** answer block is a weighted figure-left + paragraph-rail-right 2-up; page is a single content-led column in the nav shell. No literal N-equal grid anywhere.
- **One Fraunces beat (M5):** italic "first" inline in "Smile Center is cited *first* by ChatGPT and Gemini" — verdict word only, in prose, never in chrome. Textbook.
- **Blue=you / violet=agents spatial (M6/tell 8):** blue on active nav + the quoted-question left border; violet on the BEAMIX answer dot + the `#EEEAFD` GROUNDED IN provenance card. Glanceable at arm's length, violet on no button/link.
- **Mono for truth (M11):** `5`, `/6 tests`, `₪3,800/mo` all Geist Mono.
- **Signature detail (M4):** the GROUNDED-IN citation ledger (scan week + page + prompts with inline mono price) is the screen's distinctive device; "CITED FIRST · CHATGPT + GEMINI" is a signature framing. No template would ship this.

Felt depth, hairline editorial rhythm (eyebrows on hairlines, divider between verdict and prose), and provenance card all present.

## Remaining findings
**P1:** none.

**P2 (coverage, not a visible defect — does not block PASS of the captured state):**
1. Only `populated-desktop` was captured. M8 (designed empty state + two-tier recovery) and the mobile/375 breakpoint are UNVERIFIED. Capture `empty-desktop` and `populated-mobile` so the empty state (titled context + specific next step + primary blue pill + quiet secondary link + warm glyph) and mobile column collapse can be confirmed before final ship sign-off.
2. Motion (M9 entrance choreography, prefers-reduced-motion fallback) cannot be graded from a static frame — confirm in a live pass.

## One-line
PASS — confident, editorial, provenance-forward answer view; clear mono focal, one Fraunces beat, blue/violet spatial law honored; sits beside Profound/Otterly as one hand. Only gap is uncaptured empty + mobile states.
