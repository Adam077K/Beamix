# Re-critic — /settings

**Verdict: PASS**

Graded the polished render (populated-desktop.png + populated-mobile.png) against the
competitor bar (Profound / Otterly) and the 8 tells / 12 moves in CRAFT-SYSTEM.md.

## What's strong
- **Felt depth (M1).** The active "Profile / Personal information" card is a TIER-2 elevated
  white card; the "Security / Password" card below recedes onto the warm `#F7F6F2` surface as a
  `.card-inset` (1px border, no shadow). Hierarchy is felt, not uniform — tell #1 absent.
- **Intentional asymmetry (M3).** Sub-nav rail (narrow) + wide content column. No N-equal grid —
  tell #2 absent.
- **Blue=you / violet=agents applied spatially (M6).** The "THE CREW" sub-nav group (Brand
  fingerprint, Approval preferences, Publishing integrations) carries violet-tinted icons while
  Profile/Billing stay neutral; the active nav item uses the blue tint + blue left accent. The
  you-vs-agents promise is glanceable in the nav. Violet appears only on icons — never on a button
  or link. Law respected — tell #8 absent.
- **Type contract stepped (M2).** 30px InterDisplay "Settings" title → 12px uppercase muted
  eyebrows ("PROFILE", "SECURITY") → 13–15px labels/body. Registers are visibly distinct — tell #3 absent.
- **Editorial rhythm (M12).** Form rows on hairlines, tight-within-cluster / wide-between spacing;
  not one global space-y.
- **Voice + mobile.** Warm copy ("how much the crew does on its own"); mobile collapses the rail to
  a clean icon tab bar with the violet crew icons preserved. No horizontal scroll.
- No retired colors, no emojis, Inter/InterDisplay throughout, 8pt rhythm.

## Why moves M4/M5 legitimately don't apply
Settings is a forms/chrome surface with no verdict word and no score series. M5 explicitly bans
Fraunces in chrome (nav/labels/rows) and M4's sparkline needs real data points — there are none here.
Forcing a serif beat or sparkline onto a settings form would be a craft error, not an improvement.
Absence here is correct, not a tell. There is also no single number to make a TIER-1 focal, and the
two-tier card depth carries the hierarchy a focal would otherwise provide.

## Remaining findings
None at P1. Minor P2 polish notes below — non-blocking.

### P2 (non-blocking)
- The "Save changes" button sits disabled/quiet in a "No changes" footer — correct, but the disabled
  state could read slightly more intentional (current border-only ghost is fine; a faint
  surface-warm fill would tighten it). Optional.
- Avatar circle is a neutral camera-icon placeholder; consider a warmer (surface-warm) ground to
  match the empty-state warmth language. Optional.

Sits beside Profound/Otterly settings/onboarding surfaces as one hand. Ships.
