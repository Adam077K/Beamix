# Re-critic — /offsite (Off-Site Manager)

**Verdict: PASS**
**Date:** 2026-06-12
**Graded:** `screenshots-final/offsite/populated-desktop.png` (only state captured)
**Bar:** CRAFT-SYSTEM.md 8 tells / 12 moves + Profound/Otterly competitor refs

## What's strong
- **One TIER-1 focal (M1/M10):** the "Off-Site Manager" hero card with the 64px mono `62` coverage score commands; the channel rail, agent-runs rail, and source table all recede. No second hero.
- **4-step type contract is visibly stepped (M2):** `62` mono figure → "Off-Site Manager" display verdict → `BRIGHT SMILE DENTAL · RAMAT GAN` / `COVERAGE BY CHANNEL` / `RECENT AGENT RUNS` 12px tracked-grey eyebrows → 13–15px body. Gaps are obvious.
- **Earned asymmetry (M3):** dominant left column (hero + sources table) + narrower right rail. Not an N-equal grid.
- **Signature detail (M4):** micro-sparkline under the score figure.
- **One Fraunces beat (M5):** "growing" set italic-serif in "Your off-site presence is *growing*." — verdict word only, inline, never in chrome.
- **Violet is spatial (M6):** RECENT AGENT RUNS rail carries the violet agent dots; violet appears on no button or link.
- **In-cell data shading (M7):** IMPORTANCE column importance bars in score-band colors, number-dominant rows; Tracked/Untracked status pills are the canonical pill set.
- **Mono for truth (M11):** 62, 91/78/71/68/64/88/52/60, coverage %, "9 sources · +4 coverage" all tabular mono.
- **Hairline editorial rhythm (M12):** sections separated by hairlines with varied whitespace, eyebrows on hairlines.

All 8 AI tells absent. Sits beside Profound's table-with-sparkline and Otterly's clean rails as one hand — calmer and more editorial than either.

## Remaining findings (non-blocking)
- **P2 — capture coverage:** only `populated-desktop` exists. No `populated-mobile` and no `empty-desktop` render were captured, so the M8 two-tier empty state and the 375px reflow could not be visually verified. Designed-states are specified in code but not confirmed in pixels here. Capture mobile + empty before final sign-off.

No P1 issues. No retired colors (orange on the Citations channel bar is legitimate score-band data-viz, not a retired CTA accent). No violet-on-button. No N-equal grid.
