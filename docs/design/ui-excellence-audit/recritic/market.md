# Re-critic — /market (RE-CRITIC PASS gate)

**Verdict: PASS** · richness_gap 1
Graded the polished `populated-desktop.png` against the competitor bar (Profound, Otterly) and the 8 tells / 12 moves in CRAFT-SYSTEM.md.

## What's strong (preserve)
- **One TIER-1 focal, felt depth (M1).** The hero "Addressable Monthly Volume" card sits on a warm gradient surface with hero-shadow elevation; the REGION/TIMEFRAME/INTENT filter rail is a `.card-inset` (warm ground, 1px hairline, no shadow) that recedes; the Category Demand band drops to white. Three felt tiers — no uniform-depth tell.
- **4-step type contract is visibly stepped (M2).** STEP-1 `48,200` ~64px mono tabular; STEP-2 verdict "A wide-open category…" ~30px InterDisplay; STEP-3 eyebrow "ADDRESSABLE MONTHLY VOLUME" 12px uppercase tracked grey; STEP-4 body 13–15px muted. Gaps are obvious; exactly one STEP-1 figure on the screen.
- **One Fraunces beat, correctly placed (M5).** "wide-open" is Fraunces italic inline in a sans sentence, on the verdict word only — never in chrome.
- **Violet Structure done right (M6).** The agent layer is glanceable at arm's length: violet "marks an agent run" link, violet dot, and a violet "Agent promoted content" pill with a dashed violet guideline on the demand chart. Violet is the agent-annotation layer, never on a button/link-as-CTA. This is the you(blue)-vs-agents(violet) promise made spatial — the screen's signature move and its clearest separation from a generic competitor chart.
- **Earned asymmetry (M3).** Dominant left verdict column + right donut/ledger weighted 2-up; left filter rail vs wide content. No N-equal grid.
- **Mono for truth (M11).** 48,200 · +9% · 12,000 / 14,400 / 6,400 · 34k axis — all Geist Mono tabular. Prose stays Inter.
- **Accent discipline.** `#3370FF` reserved for actions; donut + chart use the data-viz series (blue/teal/green), score colors stay data-only. No retired colors, no Fraunces on light chrome.

Sits beside Profound/Otterly as one hand — arguably more opinionated than either, because the agent-annotation layer gives it a point of view neither competitor has.

## Remaining (minor, non-blocking)
- **P2 — capture gap, not a build defect.** Only `populated-desktop.png` exists in screenshots-final/market. No `populated-mobile` and no `empty-desktop`, so M8 (designed two-tier empty) and mobile single-column collapse are unverified by evidence. Capture both at 375px and the zero-data state before the screen is called fully shipped; the foundation pass makes a designed empty likely but it is not on the record.
- **P2 — donut center label.** "20,800 / UNCLAIMED" is small relative to its role as the second-most-important figure; consider nudging the mono figure up one step so the donut reads as the secondary truth-number, not a caption. Cosmetic.

No P1. No AI tell dominates. Not a clone. Reads hand-crafted.
