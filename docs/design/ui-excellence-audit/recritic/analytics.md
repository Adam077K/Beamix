# Re-critic — /analytics (polished render)

**Verdict: PASS**
Graded: `screenshots-final/analytics/populated-desktop.png` (1440×900, above-the-fold) + `populated-mobile.png` (390×844).
Bar: competitor refs (Profound citation-share line chart; Otterly clean data tables) + CRAFT-SYSTEM 8 tells / 12 moves.

## What's strong
- **One TIER-1 focal, earned asymmetry (M1/M3).** Three-zone layout: a recede filter rail (ENGINES / TIMEFRAME / TOPICS on `.card-inset` surface-warm, no shadow) + a dominant Share-of-Voice hero card (real card shadow) + a Visibility-Trend card below. Genuine narrow-rail / dominant-column asymmetry — not an N-equal grid. Exactly one hero.
- **4-step type contract is visibly stepped (M2).** Eyebrow "ANSWER-ENGINE INSIGHTS" / "SHARE OF VOICE" (12px muted uppercase tracked) → "How AI search sees you" 30px display verdict → "23" hero figure → 13–15px body. Obvious gaps between steps.
- **Serif beat, correctly placed (M5).** "You hold 23% of AI *answers* in your category" — Fraunces italic on the verdict word "answers", inline in a sans sentence, in content not chrome. Exactly one.
- **Mono hero figure + tabular ledger (M11).** "23%" reads mono; the share-of-voice ledger (You 23 / Smile Center 34 / Dental Plus 27 / Ramat Gan 11 / Others 5) is right-aligned tabular.
- **Signature data viz (M4).** Donut ring with "23% YOUR SHARE" centered, plus the annotated visibility-trend line chart ("marks an agent run") — a real differentiator that sits credibly beside Profound's citation-share view.
- **Blue/violet spatial law respected (M6).** Blue = you (hero %, donut center). Violet/blue agent-run annotation on the trend chart; violet never on a button. "Export" is a neutral secondary button (not the one blue CTA misused).
- **Filter rail is a designed surface,** not dead space — engines legend with color dots, timeframe segmented control (30d active), topic list with reset. No AI tells dominate.

## Remaining findings (minor — do not block)
- **P2 — Mobile progressive disclosure (M10).** On mobile the long filter rail (5 engines + timeframe + 6 topics) front-loads ABOVE the "23%" hero metric, burying the focal below a full scroll. Competitors lead with the headline metric. Consider collapsing filters into a sheet/disclosure on mobile so the Share-of-Voice card is the first content after the title.
- **P2 — Engine-dot color collision (watch).** ChatGPT's legend dot is blue `#3370FF` — the reserved "you" color. It's within the sanctioned data-viz series (data-1) and is a non-interactive identity dot, so not a law break, but a glanceable tension with blue=you. Consider shifting the engine legend to neutral/desaturated identity dots so blue stays unambiguously "you."
- **Capture note:** desktop is a 1440×900 above-the-fold viewport, not full-page; the visibility-trend chart is only partially visible. Focal region (where craft is decided) is fully captured and passes. A full-page capture would let us fully grade the trend chart's axis labels + the page's empty/loading states.

## Bottom line
No AI tells dominate. Clear TIER-1 focal, earned asymmetry, felt 3-tier depth, mono numbers, one Fraunces beat, signature donut + annotated trend. Reads as one hand with the dashboard exemplar and sits beside Profound/Otterly. PASS.
