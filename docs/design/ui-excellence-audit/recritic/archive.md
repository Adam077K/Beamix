# Re-critic — /archive (Run History) — RE-CRITIC PASS gate

**Verdict: NEEDS_WORK** (close — one TIER-1 depth move + capture coverage away from PASS)

Graded the polished `screenshots-final/archive/populated-desktop.png` against the competitor
bar (Profound data-table, Otterly list rows) + the 8 tells / 12 moves in CRAFT-SYSTEM.md.

## What's strong (preserve)
- **blue=you / violet=agents is spatial and glanceable (M6/tell-8 killed).** The BY column dots —
  blue for "You", violet for "Beamix" — make the you-vs-agents promise legible at arm's length
  inside a dense table. This is the single best craft move on the page and reads better than
  Profound/Otterly's undifferentiated rows.
- **4-step type contract is visibly stepped (M2/tell-3 killed).** Eyebrow "BRIGHT SMILE DENTAL"
  (12px uppercase muted) → "Run History" display verdict → body subhead → 13px row text. Obvious gaps.
- **Fraunces serif beat present (M5/tell-6 killed).** "mostly *steady*" italic verdict word inside
  the summary sentence — one beat, in content not chrome. Correct restraint.
- **Mono-for-truth holds (M11).** Run counts, 651s, $0.24, durations, step counts read tabular mono.
- **Status pills correct (warm-minimal law).** Done = green tint+saturated text, Failed = red tint —
  no loud fills. Filters + rows sit on hairline rhythm (M12). Left-aligned, no centered-in-void (tell-5 absent).
- Sits beside Profound's run/citation tables as one hand — denser-but-calmer, on-brand.

## Remaining findings
- **P2 — depth staging is flat (M1/tell-1 not fully killed).** The summary band and the table card
  carry near-identical elevation; there is no single felt TIER-1 focal. The summary strip should be
  the hero (`--shadow-card-hero`) and the table its TIER-2 surface, so hierarchy is *felt*, not just
  positioned. Right now the page reads as two equal panels stacked.
- **P2 — no signature micro-sparkline (M4).** Profound puts a trend line in its citation view; this
  list has none. The "Performance Tracker / 31 → 38%" row and the summary band's "mostly steady"
  verdict both beg for the 24px ~64px polyline of the last ~5 periods (score-band color, flat 1px
  baseline if null). Adds the one detail a template wouldn't have.
- **P2 — capture coverage incomplete.** Only `populated-desktop.png` exists. No
  `empty-desktop` (M8 two-tier designed empty cannot be verified) and no `populated-mobile`
  (375px row reflow / horizontal-scroll cannot be verified). A list view's empty + mobile states are
  load-bearing; PASS cannot be granted without them.

No AI tell *dominates*; no brand-BLOCK (palette/fonts/icons correct); not a clone.
richness_gap ~3 — route to design-polisher for the depth lift + sparkline, and re-capture
empty + mobile.
