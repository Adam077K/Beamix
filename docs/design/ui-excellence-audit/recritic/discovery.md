# Re-critic — /discovery (PASS gate)

**Verdict: NEEDS_WORK** (close — one craft-density gap + one capture gap)
**Date:** 2026-06-12 · Reviewer: design-critic
**Captured states:** populated-desktop only (mobile + empty NOT captured)

## What this screen is
The discovery-call **fallback/recovery state** ("Our live calendar isn't reachable… email us and a strategist will lock a 20-minute slot"). So this is effectively a *designed error/empty* surface — M8 territory — not a data screen. Graded as such.

## What's strong (preserve)
- **Type contract reads (M2).** Eyebrow `DISCOVERY CALL` 12px uppercase tracked grey → 30px InterDisplay verdict headline → 13–15px body → hairline → 13px footnote. Steps are visibly stepped, not flat. No evenly-weighted-type tell.
- **Fraunces beat present and correct (M5).** ONE italic serif word — *time* — inline in the sans headline "Let's find your *time* by email." On the verdict word, never in chrome. Exactly the sanctioned move.
- **Depth staging on the card (M1).** The wash-sky header panel (calendar glyph on `#EAF0FB`) recedes into the white card body with a soft TIER-2 shadow — two felt tiers inside one focal. Not uniform depth.
- **Two-tier recovery (M8).** Primary blue pill ("Email us to book") + quiet secondary ("Back to your dashboard") + a real recovery path + named contact (hello@beamixai.com). This is a *designed* error state, not a bare centered icon — the M8 bar is met in structure.
- **Blue discipline.** `#3370FF` on exactly one CTA. No violet misuse (correct — no agent zone on this surface). Accent restraint matches Profound/Otterly.
- **Warm-minimal ground.** `#F7F6F2` canvas, not grey-on-grey. Sits tonally beside the refs.

## Remaining findings

### P2 — the card is dead-center in the void (tell #5, partial)
The card is horizontally AND vertically centered in a large empty viewport — the classic "centered-in-void" composition. It's softened by the warm ground + the editorial card, so it does NOT read as a raw AI empty, but it is the one move keeping this from a confident PASS. The competitor recovery/loading states (Profound's split-screen, Otterly's left-form / right-preview) earn asymmetry even in minimal states. **Fix (no new tokens):** anchor the card off dead-center — either a left-weighted column with a calm supporting rail (e.g. "What happens next: 1→2→3" hairline list, or the strategist's name/availability), OR raise the card and add a quiet below-card reassurance row so the vertical mass isn't a single floating object in the middle of the screen. Earn the asymmetry M3 asks for.

### P2 — capture gap: mobile + empty not provided
Only `populated-desktop.png` exists. The brief expects populated-mobile (and empty-desktop where present). A recovery/CTA card must be verified at 375px (pill tap target ≥44px, no h-scroll, header glyph panel not crushed). **Fix:** capture `populated-mobile.png` so the gate has full evidence. Cannot PASS a state screen on desktop-only evidence.

## Tells scorecard
1 uniform depth — ABSENT (header panel recedes vs white body) · 2 N-equal grid — N/A · 3 even type — ABSENT · 4 no signature — soft (no sparkline, but correct for a non-data state) · 5 dead-center void — **PRESENT (partial)** · 6 serif beat — present (*time*) · 7 flat motion — unverifiable from still, not penalized · 8 blue/violet spatial — correct (blue-only surface).

**Routing:** design-polisher closes the centered-in-void asymmetry; capture mobile. Re-critic the two.
