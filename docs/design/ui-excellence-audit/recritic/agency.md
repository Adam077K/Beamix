# RE-CRITIC — /agency

**Verdict: PASS**

Graded the polished render (populated-desktop, populated-mobile) against the competitor bar
(Profound, Otterly) and the 8 tells / 12 moves in CRAFT-SYSTEM.md.

## What's strong (the craft landed)

- **Depth staging (M1) is felt, not told.** Three distinct tiers in one viewport: the "PITCH
  WORKSPACE" promo is a flat TIER-3 warm-surface inset; the "GEO AUDIT / beamix.audit" report is
  the single TIER-1 hero (header band + shadow + concentric radius); the engine breakdown reads
  as TIER-2/TIER-3. Exactly one hero. No uniform-depth tell.
- **Intentional asymmetry (M3).** Engine breakdown is a weighted 2-up — ChatGPT (Weakest) gets the
  wide focus card, Gemini + Perplexity recede as narrower stacked inset rows. The hero verdict is a
  left mono-figure / right prose split. No N-equal grid anywhere.
- **4-step type contract (M2) is visibly stepped.** STEP-1 mono figures ("4", "31", "12/100");
  STEP-2 30px display verdict ("Generate a prospect audit"); STEP-3 12px uppercase eyebrows
  ("PITCH WORKSPACE", "BY ENGINE", "TOP GAPS", "GEO VISIBILITY · /100"); STEP-4 body. Gaps obvious.
- **Serif beat (M5) — exactly one.** "Their AI visibility is *Fair*." with *Fair* in Fraunces
  italic, inline in a sans sentence, on the verdict word only. Not in chrome. Textbook.
- **Signature detail (M4).** Red micro-sparkline on the ChatGPT focus card, score-band colored
  (weakest = red). Transfers the dashboard exemplar move.
- **Mono for truth (M11).** Every number (4, 31, 12/100, 31/100, 44/100) is tabular mono; prose
  is Inter. Clean split.
- **Color law honored.** Blue #3370FF on active nav, tab underline, "Change inputs" link only.
  Amber = Fair score, red = Critical/Weakest (correct score colors, data-viz only). No violet on a
  button. The "Generate" tab is a user/pitch surface, so white/neutral (no violet zone) is correct
  here — violet structure is reserved for agent-run surfaces.
- **Mobile.** Single column, no horizontal scroll, hierarchy preserved, hero figure "31" + Fraunces
  "Fair" survive the reflow. Tab bar scrolls cleanly.

Sits beside Profound/Otterly as one hand — arguably more editorial than either (the Fraunces beat
+ the report-card framing give it a point of view the refs lack).

## Remaining findings

None at P1. Two NICE_TO_HAVE observations (do not block):

- The desktop capture is a fixed-viewport shot that cuts off mid "TOP GAPS"; a full-page capture
  would let the next pass confirm the gaps list + the bottom CTA carry the same density. Capture
  limitation, not a page defect.
- Designed empty/loading state for the "Generate" workspace not in this capture set — worth a
  spot-check that the pre-audit empty follows M8 two-tier recovery. Not gradeable from these shots.

PASS — no AI tells present, one TIER-1 focal, earned asymmetry, felt depth, mono numbers, one
Fraunces beat. Excellence bar met.
