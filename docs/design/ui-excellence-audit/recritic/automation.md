# Re-critic — /automation (polished)

**Verdict: NEEDS_WORK** (one clean P1: Fraunces serif beat absent; one P2: state-capture coverage)

Graded the polished `populated-desktop.png` (1440×900) against the competitor bar (Profound / Otterly) and the 8 tells / 12 moves. This is a genuinely strong polish pass — it sits credibly beside the refs as one hand. One required craft move is still missing, so it is not yet PASS.

## What's strong (preserve)
- **TIER-1 focal is unambiguous (M1/M10).** The "4 / AUTONOMOUS" mono card top-right is the single hero focal with hero shadow; the "THREE WAYS TO WORK" band is a correct TIER-3 warm inset (no shadow); the "BEAMIX IS RUNNING" panel reads TIER-2. Three felt depth tiers, no uniform-depth tell.
- **Violet Structure (M6) lands and is law-compliant.** The agent panel has the violet tint ground + violet hairline + violet "BEAMIX IS RUNNING" eyebrow/dot — glanceable at arm's length as the agent zone. The "Let Beamix handle it" toggle uses a violet outline + violet dot on a pale warm fill (sampled #F7F6F2), NOT a solid violet button. Blue=you / violet=agents holds; violet never on a button.
- **Signature detail present (M4).** Each agent row carries the micro-sparkline (teal/green/blue polylines, varied real shapes) — the engine sparkline transferred to this surface.
- **Earned asymmetry (M3).** Asymmetric header (title block + hero card), and the agent rows are weighted (label cluster · sparkline · toggle · action) — not an N-equal grid.
- **Type contract stepped (M2).** Eyebrow (BRIGHT SMILE DENTAL, 12px caps, muted) → H1 "Automation" (InterDisplay) → body subhead → row labels. Steps are obvious.
- **Mono for truth (M11).** Hero "4" and run-count figures read tabular mono.
- **Designed status (M8 partial).** FAQ Builder row shows the "needs your sign-off" warning pill + a left status hairline. Good in-context state.

## Remaining findings
### P1 — Fraunces serif beat absent (tell #6 / M5)
No Fraunces italic anywhere on the screen. H1 "Automation", the subhead, and the hero "AUTONOMOUS" label are all sans. The rubric requires exactly one Fraunces italic verdict/word beat per screen. Fix: set ONE editorial word in Fraunces italic inline — strongest spot is the hero card (e.g. an "Autonomous" verdict word) or a single word in the subhead ("Choose how each agent works — *you*, or Beamix on a schedule"). One beat only; never in chrome/rows/buttons.

### P2 — State-capture coverage incomplete
Only `populated-desktop.png` was captured. No `populated-mobile` and no `empty-desktop`. The two-tier designed-empty (M8) and the 375px responsive behavior cannot be verified from this render. Capture both so the empty state + mobile rhythm can be graded before final PASS.

## Bottom line
No AI tell dominates; depth, asymmetry, violet structure, mono, and the signature sparkline are all there. Add the single Fraunces beat and provide the empty + mobile captures, and this is a PASS.
