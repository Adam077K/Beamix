# Re-critic — /schema (PASS gate)

**Verdict: PASS**
**Date:** 2026-06-12
**Capture graded:** `screenshots-final/schema/populated-desktop.png` (populated-desktop only — no mobile, no empty-state capture present)

## One-line
The polished schema generator reads as a deliberately-staged, warm-minimal tool — clear TIER-1 `9/9` focal, earned 1fr/360px asymmetry, sparkline signature, one Fraunces beat, a designed live-publish success pill — it sits beside Profound/Otterly as one hand. PASS.

## What's strong
- **Depth staging (M1) is felt, not told.** The hero "Schema Generator" card carries elevation; the form below is standard TIER-2; the right rail "YOUR STRUCTURED DATA" recedes as a TIER-3 inset on surface-warm. Three real tiers, exactly one hero. Tell #1 (uniform depth) absent.
- **Type contract (M2) is visibly stepped.** `9/9` big mono figure → "Schema Generator" 30px display verdict → "BRIGHT SMILE DENTAL" tracked uppercase eyebrow → 13–15px body. Obvious gaps; nothing evenly-weighted. Tell #3 absent.
- **Earned asymmetry (M3).** Dominant content column + narrower 360px rail; no N-equal grid anywhere. Tell #2 absent.
- **Signature detail (M4) present twice and on-brand.** Blue micro-sparkline under `9/9` ("VALID · LAST RUN") and under "validity climbing" in the rail. Tell #4 absent.
- **Fraunces beat (M5).** "Your Dentist schema is *complete*" — single italic serif beat on the verdict word, inline in a sans sentence, not in chrome. Tell #6 absent.
- **Designed positive state (M8).** "Live on your site" pill: mint ground + check + real published date + truncated `<head>` location. This is the right two-tier-recovery spirit for the success case, not a bare blank.
- **Violet discipline (M6).** Violet appears only as the small "Let Beamix handle it" agent-handoff dot on the segmented control; blue "Run it myself" is the active you-state; "Generate Schema" CTA is blue. Violet is never a button. Law honored.
- **No dead-center void, real editorial rhythm (M12).** Hero → form → action bar → "View in Run History" link has varied whitespace by relationship.

## Remaining findings (P2 — non-blocking, optional polish)
- **P2 — Verify empty/error states.** Only `populated-desktop` was captured. The success pill covers the happy path, but the design-critic checklist (e) wants all four states designed with two-tier recovery. Capture the empty (no URL / first-run) and error (invalid URL, generation failed) states and confirm they carry a titled context + specific next step + primary-pill/quiet-link recovery, not a bare centered icon. Not blocking PASS on the populated render, but should be confirmed before ship.
- **P2 — Mono consistency on "17 of 20".** The free-runs counter "**17** of 20 free runs left today" reads as bold sans, not Geist Mono tabular-nums. Per M11, every real number should be mono. Switch the "17"/"20" figures to `font-mono tabular-nums` to match the `9/9` treatment.
- **P2 — Mobile capture missing.** No `populated-mobile.png`. Confirm the 1fr/360px split collapses cleanly to a single column at <768px and the action bar ("Run it myself" / "Let Beamix handle it" / "Generate Schema") wraps without overflow.

## Notes
No P1 issues. No AI tells dominate. No brand-BLOCK (accent is `#3370FF`, violet only as agent indicator, fonts on-brand). Not a clone of any competitor — original Beamix synthesis of a generator tool.
