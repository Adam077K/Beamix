# Re-critic — /shopping (PASS gate)

**Date:** 2026-06-12
**Verdict: PASS**
Graded: `screenshots-final/shopping/populated-desktop.png` (1440×900, above-the-fold — the only capture present).
Bar: competitor refs (Profound "Shopping" / citation-share + prompt tables; Otterly clean data surfaces) + CRAFT-SYSTEM 8 tells / 12 moves. Calibrated against the PASS'd dashboard + analytics exemplars.

## What's strong
- **One TIER-1 focal, three felt depth tiers (M1/M10).** The "AI Shopping Visibility" hero owns the
  fold: white elevated card with a blue top-accent hairline, big "58%" figure. The right filter rail
  (ENGINES / PRODUCT CATEGORY / TIMEFRAME) recedes to a warm-grey `.card-inset` (no shadow, TIER-3).
  The "SKU visibility" table below is a standard TIER-2 surface. One hero, no uniform-depth tell.
- **4-step type contract is visibly stepped (M2).** Eyebrow "AI SHOPPING VISIBILITY" (12px grey
  uppercase tracked) → "58%" hero figure → "Your shop shows up Often when shoppers ask AI" 30px
  display verdict → 13–15px body. Obvious gaps; nothing evenly-weighted.
- **One Fraunces beat, placed exactly right (M5).** "Often" in Fraunces italic on the verdict word
  only, inline in the sans verdict sentence. In content, not chrome. Exactly one per screen.
- **Earned asymmetry (M3).** Dominant hero + narrow filter rail; within the hero, a left verdict
  column + a right metrics column (LAST 6 WEEKS sparkline / TOP MOVER / FURTHEST BEHIND). No N-equal grid.
- **Signature detail (M4).** The "LAST 6 WEEKS 49% → 58%" micro-sparkline polyline with an endpoint
  node — the same signature data move as dashboard/analytics, transferred credibly here.
- **Blue/violet law respected (M6).** Blue "58%" is the user's own shop visibility (legitimate hero-
  metric use, same as dashboard "71" / analytics "23%"). The "+4pp vs. previous period" pill is a
  proper positive status pill (mint ground + green text). Visibility/Revenue toggle and Reset filters
  are neutral. No violet on any button.
- **Mono for truth (M11).** 58%, 49%→58%, +4pp, +11pp, −17pp, 74%, #2, ₪8,420 read as Geist Mono
  tabular; prose is Inter.
- **Designed filter rail, not dead space (M12).** Engine legend with dots, product-category list,
  timeframe segmented control (30d active), reset link — a composed recede surface, not a blank panel.
- **Sits beside the refs as one hand.** Reads like the same product as the PASS'd dashboard + analytics,
  and holds its own next to Profound's Shopping view and Otterly's data surfaces.

## Tells check (all absent)
(1) uniform depth — absent (3 tiers) · (2) N-equal grid — absent (hero + narrow rail + table) ·
(3) even type — absent (4 steps) · (4) no signature — absent (sparkline) · (5) dead-center void —
absent (asymmetric, designed) · (6) no serif — absent (Fraunces "Often") · (7) flat motion — N/A in
static capture · (8) blue/violet not spatial — blue = your-shop metric, used spatially.

## Remaining findings (minor — do NOT block)
- **P2 — Engine-dot color collision (same watch as analytics).** The ENGINES legend dots
  (ChatGPT / Gemini / Perplexity) read blue/violet — blue `#3370FF` is the reserved "you" identity
  color. They're non-interactive identity dots within the sanctioned data-viz series, so not a law
  break, but a glanceable tension with blue=you. Consider neutral/desaturated engine identity dots so
  blue stays unambiguously "you." Apply the same fix to analytics for consistency.
- **P2 — Capture completeness.** Only `populated-desktop.png` (above-the-fold) was captured. No
  `populated-mobile`, no `empty-desktop`, and the "SKU visibility" table is clipped at the fold. The
  focal region (where craft is decided) is fully captured and passes, so this does not block the gate —
  but a full-page + mobile + empty capture is needed to fully grade the SKU table's in-cell data
  shading (M7), the mobile single-column collapse (M3 fallback), and the designed empty/loading states
  (M8). Recommend re-capturing those three states against prod for a complete record.

## Bottom line
No AI tells dominate. Clear TIER-1 focal, earned asymmetry, felt 3-tier depth, mono numbers, one
Fraunces beat ("Often"), signature sparkline. The visible surface is excellent and one hand with the
dashboard/analytics exemplars. PASS — with two non-blocking P2 notes (engine-dot color, and capture
the remaining states for a complete record).
