# Re-critic — /competitors (PASS gate)

**Verdict: PASS** (with a capture/verification caveat — see below)

Graded the polished `populated-desktop.png` against the competitor refs (Profound citation-share, Otterly) + the 8 tells / 12 moves. The visible above-fold render meets the Beamix craft bar and sits credibly beside Profound's citation-share view — arguably more editorial.

## What's strong
- **One TIER-1 focal (M1/M10):** the Share-of-Voice card carries real elevation above a receded chips strip + page header. No two-hero competition. Felt depth, not told.
- **4-step type contract (M2) visibly stepped:** `23%` 64px mono figure → "Competitor Tracker" ~30px display → "SHARE OF VOICE / TRACKED COMPETITORS / BY ENGINE" 12px gray uppercase eyebrows → 13–15px body. Obvious gaps.
- **Earned asymmetry (M3):** hero is a weighted split — dominant chart column + narrower BY-ENGINE rail with a hairline divider. Header left-weighted, date pill floated right. No N-equal grid.
- **One Fraunces beat (M5):** "narrowing" set in Fraunces italic inline in the sans subhead, on the verdict word only — not in chrome. Exactly one.
- **Mono truth (M11):** all figures (23/34/28/19/31/39/26%, axis labels, +5% delta) tabular mono.
- **Signature + in-cell shading (M4/M7):** per-engine you-vs-competitor mini bars; filled area gradient under the You line; blue endpoint dot + inline "23%" label.
- **Blue=you / gray=competitor spatial law:** consistently honored across legend, chart, and engine bars. No violet on this screen, none on a button. Compliant.
- **Brand:** accent blue only; positive deltas in green status token. No retired colors/fonts.

## Remaining findings (P2 — non-blocking, verification gap)
- Only `populated-desktop.png` was captured. **No mobile (375) and no empty-state capture** exist for this page. CRAFT checklist (e) requires designed states; I cannot verify the mobile collapse or the designed empty/zero-competitors state from the evidence. Recommend capturing `populated-mobile` + `empty-desktop` to confirm the two-tier empty (M8) and mobile rhythm before final sign-off. This is a capture gap, not a proven defect in shipped craft.

No AI tells present in the visible render. PASS stands on the desktop populated state; mobile + empty remain to be screenshot-confirmed.
