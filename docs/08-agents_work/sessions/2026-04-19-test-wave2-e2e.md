---
date: 2026-04-19
lead: test-engineer (CEO-assisted commit)
task: wave2-e2e-tests
outcome: COMPLETE
branch: feat/wave2-e2e-tests
commits:
  - "test(e2e): add authenticated-dashboard + kill-switch specs"
  - "docs(env): document E2E test credential vars"
  - "test(e2e): scaffold playwright + 3 free-scan/preview/paywall specs"
---

## Summary

Scaffolded Playwright for Beamix web app. 5 E2E specs covering free-scan → preview-mode → paywall → authenticated dashboard → kill switch flows. Dev bypass via `BYPASS_TURNSTILE=true` (already supported in `scan/start/route.ts`).

## Files added

- `apps/web/playwright.config.ts` — baseURL `http://localhost:3000`, webServer runs `pnpm dev` with `BYPASS_TURNSTILE=true`, workers=1, retries=1, `list + html` reporters
- `apps/web/tests/e2e/free-scan.spec.ts` — fills /scan form, polls GET `/api/scan/[scanId]` until complete, asserts score visible
- `apps/web/tests/e2e/preview-mode.spec.ts` — from completed scan, clicks "Explore first" → asserts PreviewModeBanner → clicks "Run Agent" → asserts PaywallModal
- `apps/web/tests/e2e/paywall-modal.spec.ts` — 3 tiers shown, Build highlighted, annual toggle works
- `apps/web/tests/e2e/authenticated-dashboard.spec.ts` — Supabase test user sign-in (env-gated; skip if `E2E_TEST_EMAIL` unset) → /home + /inbox + /scans accessible
- `apps/web/tests/e2e/kill-switch.spec.ts` — /automation kill switch button + confirmation modal
- `apps/web/.env.example` — added `E2E_TEST_EMAIL`, `E2E_TEST_PASSWORD`, `BYPASS_TURNSTILE=true`

## Dependencies

- `@playwright/test@^1.59.1` installed in `apps/web`

## Verification

- `playwright test --list` not executed (worktree-level `pnpm install` required first)
- Chromium not downloaded (deferred; run `pnpm exec playwright install chromium` on next dev machine)
- Typecheck not run — same node_modules caveat

## Known gaps / follow-up

- Run `pnpm install` at repo root + `pnpm -F @beamix/web exec playwright install chromium` before first CI run
- Create a dedicated Supabase E2E user and store credentials in GitHub Actions secrets
- Add CI job (GitHub Actions) to run `pnpm playwright test` against staging
