# Handoff — agent system rebuild, 2026-08-09

Paste the block below into a fresh CEO session to continue.

---

```
You are the CEO/orchestrator for Beamix. Read .claude/agents/ceo.md for your role.

MISSION: continue the clean-sheet re-architecture of Adam's agent system — a CROSS-PROJECT
asset used in ~10 projects, not tooling for one repo. The DESIGN IS LOCKED. Nothing has been
built. Do not restart the research; six research and audit runs (~12M tokens) are already done
and their conclusions are on main.

READ FIRST, IN THIS ORDER:
1. docs/08-agents_work/2026-08-09-AGENT-SYSTEM-ARCHITECTURE.md   <- the design. Start here.
2. docs/08-agents_work/2026-08-09-AGENT-SYSTEM-REBUILD-PLAN.md   <- substrate: 9 components,
   build sequence, stop conditions
3. docs/08-agents_work/2026-08-09-target-system-spec/            <- 8 files, 4825 lines, the
   complete target enumerations (agents 26->31, skills 149->109, MCPs 13->9, workflows 6->4,
   plus hooks, system prompts, commands, memory)

Supporting, read only if relevant to your task:
- 2026-08-09-skill-harvest/ (510 path-verified items from 12 external sources)
- 2026-08-09-adaptive-system-design-research.md (QM = the YC harness, GSD, 5 design questions)
- 2026-08-09-prime-agent-research-brief.md
- sessions/2026-08-08-ceo-agent-system-clean-sheet-rethink.md (9-surface audit, 88 findings)

ADAM'S PRINCIPLE — everything follows from this:
"Constrain outcomes, not methods." His words: "give the worker and the other agents some
freedom to explore new ways to handle things, solve them, get different context — they get a
task and they need to do it in a high quality. so we need to give them space to work."
He has consistently chosen autonomy over gating: advisory cost, no build cap, no review queue,
advisory capability envelope. Do NOT keep proposing gates. Where a gate exists it is because
being wrong there is unrecoverable.

STATE:
- PLANNING ONLY. Nothing is built until Adam says build. He said "planning only now."
- No hard cap on the rebuild ("work until it feels right"). The 7 stop conditions in the plan
  are therefore the only brake, and must ship as checked artifacts, not sentences.
- Everything is merged to main: PR #193, PR #194. Working tree clean.

FACTS VERIFIED BY EXECUTION THIS SESSION — do not re-derive, do not design against the old
assumptions:
- Nested subagent spawning WORKS (live depth-2 test returned DEPTH_2_ALIVE). The block that
  justified the whole "chiefs are planning-only / dispatch packet" apparatus was last tested 72
  runtime versions ago and is FALSE. Do not rebuild dispatch packets.
- The QA gate is BOTH forgeable AND unenforced. Its PASS decision is a grep for a hand-typed
  string, qa.js is never invoked by the workflow, branch protection returns 404, and ruleset
  13276203 has enforcement: disabled. Both halves must be fixed together.
- mcpServers: frontmatter was NEVER the enforcement point. tools: is, and ZERO of 26 agents
  have any mcp__ entry there. database-engineer instructs itself to call
  mcp__supabase__execute_sql seven times while lacking the tool.
- The MANIFEST generator has three YAML parsing bugs corrupting 62 of 149 descriptions (41%).
  The source SKILL.md files are FINE. Fix the generator, not the skills.
- The Workflow runtime has NO module-import support, which is why gate-logic.mjs can never be
  imported by qa.js and survives as hand-mirrored copies.
- Codex CLI is NOT installed and not on the bash allowlist, despite CLAUDE.md documenting it in
  the Full-tier pipeline. Adam decided to actually wire it (decision 14).
- All 146 skill descriptions cost only ~7,400 tokens, so the CEO holding "all the lists" is
  cheap and affordable.

NEXT ACTIONS, in order:
1. Re-run the skills cut list. Its 30 cuts used "wrong-stack for Beamix"; decision 12 changed
   the test to "useless in EVERY project" (stripe-integration and clerk-auth are wrong for
   Beamix and possibly right for a sibling repo). That workflow ran before the decision.
2. Finish the grill on the branches not yet covered: model routing per role, worker BLOCKED
   handling, how work arrives.
3. Then, only when Adam says build: start at step 0 of the plan's sequence — close the lapsed
   vindication triggers, which CLAUDE.md still claims are active eight weeks after expiry.
4. If only one thing gets built: fix the QA gate. Anything can merge right now, including work
   from this rebuild.

OPERATIONAL LESSONS — these cost real tokens today, do not repeat them:
- NEVER ask an agent for a large enumeration inside a structured return. Twelve agents each did
  full research and returned NOTHING because a ~13KB payload failed to parse, five retries each.
  Have agents WRITE the payload to a file and return only a pointer plus counts. This pattern
  then worked perfectly twice.
- Guard against dropout BEFORE a synthesis stage. A collapse stage ran on an empty inventory and
  produced a detailed, plausible, entirely fabricated catalog. If the input is empty, refuse.
- Agents stall near ~180k tokens and return nothing. Tell them explicitly to finish inside
  budget, and that a partial structured return beats an unwritten perfect one.
- Verify claims before repeating them. Two load-bearing numbers from an earlier audit were wrong
  ("8 gsa-core sync PRs" — there is 1; "4+ branches with QA-reviewed work" — there are 0).

RULES:
- Research delivered only in conversation does not exist. Write it to a file. A prior Prime
  Agent research pass was lost exactly this way and had to be redone.
- Any rule whose enforcement is "the agent should remember" is disqualified by construction.
  Name a hook, a CI job, a resolver, or a data file the code reads.
```

---

## Why this handoff exists

Two prior sessions lost work by leaving conclusions in conversation. The Prime Agent research
was dismissed as irrelevant *and* never written down, so it had to be redone from scratch. Two
handoff files sat on an unmerged branch where no new session could find them.

Everything referenced above is on `main` as of 2026-08-09.
