# Handoff — agent system rebuild, 2026-08-09 (end of day)

Paste the block below into a fresh CEO session to continue.

---

```
You are the CEO/orchestrator for Beamix. Read .claude/agents/ceo.md for your role.

MISSION: continue the clean-sheet re-architecture of Adam's agent system — a CROSS-PROJECT
asset used in ~10 projects, not tooling for one repo.

**THE 23 DECISIONS ARE LOCKED AND PLANNING IS COMPLETE** as of end-of-day 2026-08-09. Every
design question is answered and every one of the 10 components names a mechanism — a hook, a CI
job, a resolver, or a data file. Do not re-open the design; build it.

Three gaps remain and they are SPEC/SCHEDULING, not design — listed under PLANNING GAPS below:
which commands CI actually runs, three unscheduled classifier holes, and a skills cut list with
no list behind it.

Do not restart the research; seven research/audit runs are done and their conclusions are on
main. The build has STARTED — step 1 of the sequence is merged.

READ FIRST, IN THIS ORDER:
1. docs/08-agents_work/2026-08-09-AGENT-SYSTEM-ARCHITECTURE.md  <- the design, 21 decisions
2. docs/08-agents_work/2026-08-09-AGENT-SYSTEM-REBUILD-PLAN.md  <- 9 components, sequence,
   stop conditions. Note the amendments dated 2026-08-09 — component 3 grew a THIRD half.
3. docs/08-agents_work/2026-08-09-hook-audit/SYNTHESIS.md       <- why the hook-gap thesis
   died and what replaced it
4. docs/08-agents_work/2026-08-09-target-system-spec/           <- 8 files, target enumerations

Supporting, only if relevant:
- 2026-08-09-reference-systems-local.md — 24 external systems cloned to ~/VibeCoding/_reference/
- 2026-08-09-pydantic-ai-research.md — mechanism harvest, 3 portable mechanisms
- 2026-08-09-skill-harvest/ (510 items, 12 sources)
- sessions/2026-08-09-ceo-agent-system-rebuild.md — the full session record

ADAM'S PRINCIPLE — everything follows from this:
"Constrain outcomes, not methods." He has consistently chosen autonomy over gating: advisory
cost, no build cap, no review queue, advisory capability envelope. Do NOT keep proposing gates.
Where a gate exists it is because being wrong there is unrecoverable.

STATE — WHAT CHANGED 2026-08-09:
- THREE PRs MERGED, all irreversible tier, all 3-of-3 reviewer coverage:
    #198 bd8e7d0 — qa-tier-floor pattern gaps closed
    #196 0471927 — settings.json.proposed hook registrations restored
    #197 46f4ddd — schema-lint wired into CI + stale maxTurns bound corrected
- STEP 1 OF THE SEQUENCE IS DONE. schema-lint.js now runs on every PR (26 pass, 0 fail).
  This repo's CI executes code against a diff for the first time.
- Decisions 15-21 locked (see ARCHITECTURE doc). 15 model routing · 16 BLOCKED handling ·
  17 work arrival · 18 capability envelope · 19 acceptance criteria · 20 propagation ·
  21 run-log consumption.
- Cross-project fit MEASURED, no longer an open question: 11 repos carry the system, the 3
  without a tier-floor are exactly the 3 with zero commits in 90 days (out of scope), and
  etsyc (689 commits/90d) + evalove (385) both OUT-COMMIT Beamix. ROI was understated.

PLANNING GAPS — BOTH CLOSED 2026-08-09 (decisions 22 and 23). Kept here for context only; do
not re-decide them. Every design question is now answered and every component names a mechanism.

CLOSED — 22. **Context injection is its own component** (plan component 10): one advisory hook
reading `.claude/context-injection.yml`, never blocking. It already had members decided
elsewhere — decision 21's reader-staleness warning, decision 18's envelope-reach feedback, a
tier warning on opening an irreversible file — which is why it is one data-driven component and
not five hand-rolled hooks. Highest-value entry: inject the relevant DECISIONS.md entry when an
agent edits a file that decision covers, turning "read before acting" and "leave breadcrumbs"
from prose into context. Accepted cost: injected context is paid in tokens on every matching
call, in ~10 repos, and this plan has never priced context cost.

CLOSED — 23. **The self-improvement pilot is proposed by the decision-21 reader**, gated on the
run log holding >= 4 weeks of data. Signal: an envelope reach means composition missed a skill
the worker needed; a loaded-but-unused skill means the description oversold it. One PR/week,
dedicated branch, capped at N edits, 100% human review. Adds no new mechanism, so stop condition
7 never starts a clock.

The original framing of both, for reference:

1. **ADVISORY CONTEXT HOOKS — a new component with no home.** The 2026-08-09 hook audit found
   that 31 of GSD's 47 hooks are ADVISORY: they inject `additionalContext`/`systemMessage`,
   cache state, or scan output. They shape what an agent KNOWS, not what it MAY DO. Beamix has
   exactly one (`gsa-context-monitor.js`). This is the single genuinely new idea the 24 cloned
   reference systems produced, and it fits "constrain outcomes, not methods" BETTER than any
   gate currently in the plan — it widens what a worker knows without narrowing what it may try.
   **The plan has no component for it.** Decide: new component, folded into an existing one, or
   rejected with a reason. Evidence is in 2026-08-09-hook-audit/ (both GSD catalogues) and the
   libraries are on disk at ~/VibeCoding/_reference/harness/.

2. **THE SELF-IMPROVEMENT PILOT HAS NO MECHANISM.** Locked decision 5 in the plan specifies the
   POLICY — skill descriptions only, one worker type, dedicated branch, rate-capped, 100% human
   review, measuring how often the automated check alone would have let a bad edit through. It
   does NOT specify HOW an agent proposes a description edit: no hook, no CI job, no resolver,
   no data file is named. By the plan's own disqualification rule this is prose. Decide the
   mechanism, or explicitly defer the pilot until after the run log has data.

THREE SMALLER GAPS, spec/scheduling rather than design:
3. Component 3's third half says "CI must execute code against the diff" — but not WHICH.
   `tsc`? `eslint`? `pnpm test`? `pnpm build`? `pnpm audit`? On what trigger, failing how?
4. The three classifier holes (.mcp.json, .claude/commands/**, .claude/workflows/**) are decided
   to fix but not scheduled into the sequence.
5. Skills 149→109 is a target with no list behind it. Criteria decided ("useless in EVERY
   project"), test now runnable against 24 cloned systems + 9 live repos, work not done.

NEXT ACTION — after the two planning gaps above are closed:
**COMPONENT 3 — make the QA gate real.** Three halves, all verified broken:
  (a) the verdict is a grep for a hand-typed `qa_verdict: PASS` string in a markdown file.
      The CEO merged all three PRs above by typing it. Replace with a SHA-bound
      qa-verdict.json; CI asserts commit_sha == head.sha.
  (b) ruleset 13276203 has enforcement: disabled — nothing is a required check. Turn it on.
  (c) NO CI job runs tsc, eslint, pnpm test, pnpm build or pnpm audit. A forgery-proof
      verdict on a gate that compiles nothing is still theatre. Make it execute code.
  (d) ALSO: qa-lead-pass.yml's tier-floor hard-fails ONLY at irreversible. A `full` floor
      prints an info line and lets a forged `tier: lite` session file merge. Second forgery
      path — close it with the others.
All halves land together or none matters.

THEN, in order: step 2 (extend schema-lint to tools:/mcpServers: + a description lint),
step 3 (tier-floor advisory lane + provenance axis), 3.5 (gsa-sync --check across the 9 live
repos), then the rest of the sequence in the plan.

STILL OPEN, carried:
- THREE CLASSIFIER HOLES. `.mcp.json` resolves to `lite` — it grants every agent's tool and
  API access, blast radius comparable to `.claude/hooks/**` which is already irreversible.
  `.claude/commands/**` also lite. `.claude/workflows/**` (the QA gate's OWN scripts, 10
  files) still lite; a fix exists on unmerged branch feat/spec-conformance-and-qa-lead-accuracy
  (09b81ee) and conflicts textually with the merged #198.
  (Decision 21's hole is CLOSED as of 2026-08-09 — the reader appends {event:'reader_ran', ts}
  to the run log, and a far-more-frequent path (CEO session start, or schema-lint in CI, which
  now runs on every PR) warns loudly when that stamp goes stale. No new mechanism, so stop
  condition 7 never starts a clock on it. It warns rather than blocks — accepted.)
- `trivial` is STRUCTURALLY UNREACHABLE: the `**` catch-all sits at lite and the resolver takes
  MAX rank, so no path resolves to trivial. Needs a resolver algorithm change. Filed at
  component 3.
- Handoff action #1, the skills cut list, still not re-run — but it is no longer blocked on
  judgment. 24 reference systems are cloned (1,150 SKILL.md vs our 146) and gsa-sync --check
  at step 3.5 produces the cross-repo inventory the "useless in EVERY project" test needs.

FACTS VERIFIED BY EXECUTION — do not re-derive:
- The tier-floor resolver is HIGHEST-TIER-WINS (max rank, no break, qa-lead-pass.yml:215-217).
  Rule ORDER HAS NO EFFECT. The file's own header comment said first-match-wins and was wrong;
  a 2026-05-29 session found this and it was never fixed. Fixed in #198.
- The resolver SUBSTRING-MATCHES; it does not parse YAML. Any line containing `- pattern:` or
  `tier:` is read as a rule, INCLUDING COMMENTS. A worker's own draft comment tripped this and
  silently promoted every file in the repo to tier `full`. Component 3 must replace the
  substring scan with real YAML parsing.
- Nested subagent spawning WORKS. Do not rebuild dispatch packets.
- `tools:` — not `mcpServers:` — is the MCP enforcement point, and ZERO of 26 agents have an
  mcp__ entry there. Three MCPs are declared and unconfigured: linear (23 files), context7
  (13 files), both absent from .mcp.json which contains only supabase.
- The MANIFEST generator has three YAML bugs corrupting 62 of 149 descriptions. The source
  SKILL.md files are FINE. Fix the generator.
- The Workflow runtime has NO module-import support.
- Beamix states 63 rules and enforces 9. Forty are unenforced-but-mechanizable; fourteen are
  inherently judgment and should stop being written as rules.
- Codex CLI is NOT installed. Decision 14 wires it.

OPERATIONAL LESSONS — these cost real tokens, do not repeat them:
- NEVER ask an agent for a large enumeration inside a structured return. Have agents WRITE the
  payload to a file and return a pointer plus counts. Briefed that way, three audit workers
  landed cleanly; the one agent NOT briefed that way (qa-lead) stalled and returned nothing
  after doing full work. Natural experiment, same day.
- Guard against dropout BEFORE a synthesis stage. If the input is empty, refuse.
- Tell agents explicitly to finish inside budget, and that a partial structured return beats an
  unwritten perfect one. It works — every agent so instructed returned.
- Tell reviewers what NOT to flag. Three reviewers were told the resolver is max-rank and that
  trivial-unreachability is out of scope; without that they would each have rediscovered a
  correction already made.
- When briefing a worker on a premise, add "do not assume my description over what the file
  says." That single line caught a wrong premise the CEO had asserted as critical.

THE METHOD RULE THAT EARNED ITSELF TODAY:
**A count produced by pattern-matching is a hypothesis, not a finding.**
Seven claims were checked against implementing files on 2026-08-09. All seven were wrong, and
every one overstated reality:
  8 gsa-core sync PRs -> 1                    (prior audit)
  4+ QA-reviewed branches -> 0                (prior audit)
  78 hooks vs our 7 -> ~3 vs 1 live blocking  (CEO, grep artifact)
  23 blocking vs 3 -> same                    (CEO, grep artifact)
  schema-lint "already passing" -> 16/10 fail (subagent)
  tier-floor "first-match-wins" -> max-rank   (CEO, inherited from the file's own comment)
  "a second parser hazard" -> there was one   (CEO, relayed a worker's phrase unverified)
Three were the CEO's, including one written INTO the plan document describing how to prevent
exactly this. The lesson is not "be more careful." It is that a relay without a resolver
produces fabrications regardless of who relays. This is decision 8's case, made seven times in
one day.

RULES:
- Research delivered only in conversation does not exist. Write it to a file.
- Any rule whose enforcement is "the agent should remember" is disqualified by construction.
  Name a hook, a CI job, a resolver, or a data file the code reads.
- Verify claims by reading the implementing file before repeating them. See above.
```

---

## Why this handoff exists

Two prior sessions lost work by leaving conclusions in conversation. Everything referenced above
is on `main` as of 2026-08-09.

## What the 2026-08-09 session produced

- **Decisions 15-21** locked via nine `/grill-me` questions, closing every branch the previous
  handoff left open plus four the grill surfaced.
- **Three merged PRs**, irreversible tier, 3-of-3 reviewer coverage each. Step 1 of the sequence.
- **A hook audit that killed its own thesis.** Commissioned to close a 78-vs-7 hook gap; found the
  gap does not exist (3 vs 1 live blocking hooks) and that the real number is 63 rules stated, 9
  enforced. Also found that declared-never-wired is not a Beamix pathology — 5 of gsd-core's 31
  hooks are shipped, config-gated and registered nowhere.
- **Five holes in one classifier**, found by five separate passes, none by reading the file. Each
  surfaced only when something with teeth was pointed at it, and the count rose every time.
- **24 reference systems cloned** to `~/VibeCoding/_reference/` (406M, 1,150 `SKILL.md`).
- **A Pydantic AI mechanism harvest** that re-diagnosed our silent-failure bug: their contract
  raises a typed exception on retry exhaustion and never returns nothing, so our 12-agents-
  returned-nothing incident is a swallowed signal, not only an oversized payload. Also confirmed
  decision 19's command/judge split was independently arrived at by another team.
