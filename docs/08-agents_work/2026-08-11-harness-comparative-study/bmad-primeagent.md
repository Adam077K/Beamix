# Harness Comparative Study — BMAD-METHOD, agent-os, prime-agent, pi

Status: COMPLETE (all four repos, three special targets, cross-repo comparison table, and a "what I did not get to" section below).

Repos studied (read-only, local clones):
- `~/VibeCoding/_reference/harness/bmad-code-org_BMAD-METHOD`
- `~/VibeCoding/_reference/harness/buildermethods_agent-os`
- `~/VibeCoding/_reference/harness/PrimeIntellect-ai_prime-agent`
- `~/VibeCoding/_reference/harness/earendil-works_pi`

Every claim below is tagged VERIFIED (file opened and read) or ESTIMATED (grep/pattern-match only, not opened). Untagged claims should be treated as ESTIMATED.

---

## 1. BMAD-METHOD

**Shape.** BMAD is not agent-hierarchy software in the classic sense — it's a filesystem-based skills library conforming to the "Agent Skills open standard" (`tools/skill-validator.md`, VERIFIED). "Agents" (bmad-agent-pm, bmad-agent-dev/"Amelia", bmad-agent-analyst, bmad-agent-architect, bmad-agent-ux-designer at `src/bmm-skills/agents/*/SKILL.md`, VERIFIED) are personas expressed as SKILL.md files with a `name`/`description` frontmatter and markdown body — loaded on demand by whatever LLM harness reads them (Claude Code, or anything else that can read files and run `uv`). There is no agent-spawning runtime shipped by BMAD itself for the core PM/dev/architect roster — those are single-persona prompt-swaps, not subagents.

**1. ORCHESTRATION.** The one place BMAD does spawn multiple concurrent agents is `bmad-party-mode` (`src/core-skills/bmad-party-mode/SKILL.md`, VERIFIED), a roundtable/panel skill. It has four modes set by `{workflow.party_mode}` or a `--mode` flag: `session` (one LLM voices every persona inline — the default fallback), `auto` (voice inline for ordinary exchanges, spawn real subagents "only when independent thinking changes the outcome" — decision logic lives in `references/mode-auto.md`, not yet read), `subagent` (`references/mode-subagent.md`, VERIFIED — spawns one agent per persona, keeps the cast alive across turns "where your harness can supports it", weaves parallel replies into conversational order afterward), and `agent-team` ("Claude Code only", stands personas up as a persistent team). This is the only place in BMAD where "how much machinery a task gets" is a real decision, and it is NOT recorded anywhere durable — `mode-subagent.md` explicitly says the fallback (`session`) is used "without comment" when a harness lacks spawn capability, and there is no manifest/log entry of which mode ran. The rest of BMAD's "orchestration" is a human or top-level LLM picking which single-persona skill to invoke next (e.g. bmad-agent-pm → bmad-prd skill); there is no dispatcher agent deciding tier/scope programmatically.

**2. ENFORCEMENT — nothing blocks at runtime.** `grep -rn "settings.json\|PreToolUse\|PostToolUse"` across the whole repo (excluding node_modules) returns zero hits (VERIFIED), and there is no hooks directory outside `.git/hooks`. BMAD ships zero PreToolUse/PostToolUse-style hooks — everything is prose instruction to the LLM (e.g. STEP-04 "Halt Before Menu" in the skill validator is a *convention checked by an LLM validator*, not a programmatic gate). The one real, code-enforced mechanism in the whole repo is the installer's file-integrity/backup logic (see Target #1 below) — that runs at `bmad install`/`bmad update` time, not during agent execution. A second quasi-enforcement layer is CI: `npm run validate:skills` runs `tools/validate-skills.js` deterministically (13 of 26 rules — SKILL-01..07, PATH-02, STEP-01/06/07, SEQ-02, TPL-01) plus an LLM-inference pass for the rest (`tools/skill-validator.md`, VERIFIED), wired into `.github/workflows/quality.yaml` per `AGENTS.md` ("Rules" section, VERIFIED) — this blocks a PR merge, not an agent's live action.

**3. CAPABILITY DECLARATION — does not exist.** `grep -rln "allowed-tools\|allowed_tools"` across the entire repo returns 0 matches (VERIFIED). No skill or agent declares which tools it may use; there is no capability manifest to check against, decorative or otherwise. This is a genuine absence, not a hidden-and-unchecked field.

**4. MEMORY/STATE.** Two independent mechanisms: (a) `src/scripts/memlog.py` (VERIFIED, read first 60 lines) — an append-only, atomic (temp-file + fsync + rename), write-only `.memlog.md` log used by party-mode and other skills to persist session state across runs. Its own doc explicitly states the "write-only / blind" invariant: "the caller never re-reads the file mid-session — the one time the file is read is on resume, and the caller reads it itself, not via this script." There is no resolver that checks a memlog claim against current repo/filesystem reality — it is trusted verbatim. (b) `_bmad/_config/manifest.yaml` / `config.toml` — installer-owned state about what's installed (modules, versions, agent roster), regenerated on every install, explicitly documented as "installer-managed... direct edits will be overwritten" (`manifest-generator.js` lines 497-524, VERIFIED).

**5. PROPAGATION/INSTALL — real per-file SHA256 manifest, partial protection for edits.** See Target #1 below for the full mechanism (VERIFIED by reading `tools/installer/core/manifest-generator.js` and `tools/installer/core/installer.js`).

**6. OBSERVABILITY — none found.** No run log, telemetry, or `.jsonl` event stream anywhere in `tools/` or `src/` (grep for `run_log|telemetry|\.jsonl` across `.js`/`.py`/`.md`, 0 hits, VERIFIED). The closest thing is the memlog above, which is write-mostly narrative state, not structured telemetry, and nothing reads it except the skill that wrote it on its own resume.

**7. STEAL / REFUSE.** Steal: the `files-manifest.csv` per-file SHA256 hash + "diff against last-known-good before overwrite" pattern in `tools/installer/core/installer.js::detectCustomFiles()` — cheap, deterministic, and it correctly separates "file the user never touched" (silently updated) from "file the user edited" (flagged and backed up) without needing any semantic understanding of the file. Refuse: shipping an entire methodology as pure prose convention with zero programmatic enforcement (Q2 above) while using CRITICAL/HIGH/MEDIUM severity language in `tools/skill-validator.md` that reads like a real gate — the severity labels imply consequences that don't exist at runttime; only a CI lint catches violations, and only for repo-committed skills, never for an agent's live behavior mid-session.

### Target #1 — BMAD per-file manifest: generation, checking, and modified-file handling (the whole point)

**Generation:** `ManifestGenerator.writeFilesManifest()` in `tools/installer/core/manifest-generator.js:681-741` (VERIFIED, read in full) writes `_bmad/_config/files-manifest.csv` with columns `type,name,module,path,hash` on **every** install and update. The hash is `crypto.createHash('sha256').update(fileContent).digest('hex')` computed fresh over every installed file (`calculateFileHash()`, lines 669-676).

**When checked:** On update only (not on fresh install — there's nothing to diff against). `Installer._prepareUpdateState()` (`installer.js:590-619`, VERIFIED) calls `readFilesManifest()` to load the *previous* run's CSV, then `detectCustomFiles(bmadDir, existingFilesManifest)` (`installer.js:844-955`, VERIFIED) walks the live `_bmad/` directory and classifies every file into one of three buckets **before any files are overwritten**:
  - **Custom** (not present in the previous manifest at all — i.e., the user added it): fully preserved. Backed up to a temp dir, and after the update completes, copied back verbatim to its original path (`_restoreUserFiles()`, `installer.js:519-551`, VERIFIED) — `overwrite: true` on the restore, so the user's net-new file wins completely.
  - **Modified** (present in the previous manifest, but its live SHA256 no longer matches the recorded hash — i.e., the user edited a file BMAD shipped): backed up to a temp dir, but **NOT restored to the live path**. After the update overwrites the file with the new shipped version, the *old, user-edited* content is written out as a sibling `<path>.bak` (`installer.js:553-568`, VERIFIED: `const bakPath = modifiedFile.path + '.bak'`). The message shown to the user is literally "Restoring N modified files as .bak..." — restoring the backup, not the live file.
  - **Unchanged** (hash matches): silently overwritten with the new version, no fanfare.

**Answer to "how does it handle a legitimately modified file":** it does **not** preserve the live edit in place. The new shipped version always wins at the original file path; the user's prior edit survives only as a `.bak` file next to it, which the user must notice and manually re-apply/diff. This is a real, working conflict *detector* (SHA256 diff before overwrite) but only a partial conflict *resolver* — it never attempts a merge and never leaves the user's version live. Net-new files (true "custom files," e.g. a whole file the user added that BMAD never shipped) get the strictly better treatment: those ARE fully restored verbatim. One more wrinkle: `manifestHasHashes` gates modification-detection — if an old manifest was written before the hash column existed, `detectCustomFiles` silently skips modification detection entirely for that upgrade path (line 855-858, `manifestHasHashes = existingFilesManifest.some(f => f.hash)`), so a manifest without hashes provides zero protection, not a fail-safe warning.

---

## 2. buildermethods/agent-os

**Shape.** Confirmed by direct file count (VERIFIED — `find . -path ./.git -prune -o -type f -print | wc -l` = 22): agent-os v3.0 ships exactly 22 files total. There is no `agents/` directory anywhere in the repo, and `grep -rln "subagent" .` across every file returns exactly one hit — `CHANGELOG.md` — a historical reference to the old system, not a live one (VERIFIED). The whole product is: `config.yml` (profile config), 3 shell scripts (`scripts/project-install.sh`, `sync-to-profile.sh`, `common-functions.sh`), 5 Claude Code slash-command prompt files (`commands/agent-os/{plan-product,shape-spec,discover-standards,inject-standards,index-standards}.md`), and one seed standards file (`profiles/default/global/tech-stack.md`). This is a **standards-injection tool**, not an orchestration framework.

**1. ORCHESTRATION — none; explicitly delegated to the host tool.** There is no dispatcher, no roster, no spawn logic anywhere in the repo. `install_commands()` in `project-install.sh:384-411` (VERIFIED, read in full) literally just `cp`s the 5 command `.md` files into `.claude/commands/agent-os/` in the target project — that's the entire "orchestration install." `inject-standards.md` (VERIFIED, read in full) is representative of the whole command set: it's a prose runbook that tells the calling LLM to check if it's in Claude Code's plan mode, ask the user via `AskUserQuestion` when uncertain, read `index.yml`, and paste standards into context. No decision about "how much machinery a task gets" exists to record, because the framework does not touch task/agent scoping at all — v3's own changelog states this is deliberate (see Target below).

**2. ENFORCEMENT — none.** `grep -n "allowed-tools\|tools:" commands/agent-os/*.md` returns zero hits (VERIFIED). No hooks, no settings.json, no PreToolUse/PostToolUse anywhere (only the standard git sample hooks under `.git/hooks`, unmodified). The closest thing to a gate is a single interactive shell prompt in `project-install.sh::confirm_standards_overwrite()` (lines 164-189, VERIFIED) — "Existing standards folder detected... overwrite? (y/N)" — a whole-folder yes/no, not a per-file check, and it only fires at install time, never at agent runtime.

**3. CAPABILITY DECLARATION — does not exist.** Same as BMAD: no frontmatter field anywhere restricts what a command/agent may use. Commands are plain prose read by whatever tool invokes them.

**4. MEMORY/STATE.** `agent-os/standards/index.yml`, generated by `create_index()` in `project-install.sh:278-382` (VERIFIED) — an AWK-scanned description index that preserves prior human/LLM-written descriptions across re-index runs (looks up the old index by folder/filename before falling back to "Needs description - run /index-standards"). That's the only persistent state agent-os introduces; specs/product docs are the user's own files, not framework-managed state. No resolver checks a memory claim against repo reality — `index.yml` descriptions are trusted as written.

**5. PROPAGATION/INSTALL — blunt, whole-folder, no per-file protection.** `install_standards()` (`project-install.sh:204-276`, VERIFIED) walks each profile in the inheritance chain and does a straight `cp "$file" "$dest_file"` for every `.md` — no hash, no diff, no per-file conflict detection at all. The only safety net is the single confirm-or-abort prompt covering the *entire* standards folder (`confirm_standards_overwrite()`) — a customization to one file gives the same warning, and same fate, as a customization to fifty. This is markedly weaker than BMAD's per-file SHA256 approach (see BMAD section above): agent-os protects nothing at file granularity, only offers an all-or-nothing bail-out before the whole folder is clobbered.

**6. OBSERVABILITY — none found.** No log file, no run history, nothing written that isn't `index.yml` (a content index, not telemetry).

**7. STEAL / REFUSE.** Steal: `inject-standards.md`'s three-scenario detection (conversation / building a Claude Skill / in plan mode) with an explicit "ask, don't assume" rule when the scenario is ambiguous — a small, well-scoped pattern for injecting the right amount of context without over- or under-loading it. Refuse: the whole-folder single-prompt overwrite in `install_standards()` — a project with even one customized standards file gets an all-or-nothing choice between "lose my edit" and "block the whole update," which is strictly worse than doing nothing at file granularity; BMAD's per-file hash diff (same problem domain) is directly copy-able and better.

### Target #2 — the roster removal, verified, and why (their words, not a summary)

**Verified fact:** current `main` (VERIFIED via file listing above) has zero agent-roster files of any kind. This is not merely "moved" — there is no `agents/`, `roles/`, `implementers.yml`, or `verifiers.yml` anywhere in the tree.

**The history, from `CHANGELOG.md` (VERIFIED, read in full through v2.1.0 and v3.0 entries) — two separate removals, four months apart:**

- **v2.1.0 (2025-10-21) retired the first roster** — a "Roles" system (`roles/implementers.yml` and `roles/verifiers.yml`, "convoluted lists of agents that could be assigned to implement tasks," plus a script for adding more roles). Their stated reason, verbatim: *"That system added no real benefit over simply using available tooling (like Claude Code's own subagent generator) for spinning up your subagents."* They replaced it with an `orchestrate-tasks` phase for ad hoc multi-agent work, keeping a lighter-weight `use_claude_code_subagents: true/false` boolean rather than a curated roster.
- **v3.0 (2026-01-20) removed orchestration/implementation entirely**, not just the roster. Their stated reason, verbatim: *"AI coding tools have evolved significantly since Agent OS's original release in mid-2025. Claude Code's plan mode, extended thinking, and improved models now handle much of the scaffolding that earlier versions provided... Implementation/orchestration phases retired — frontier models handle this well on their own now."* The v3 changelog frames this explicitly as *not reinventing what the host tool already does well* — the framework "refocuses... on what it does best — establishing and injecting standards — while deferring to modern AI tools for the parts they now handle better."

**Reading:** this reads as an explicit, twice-repeated bet that model capability improvements (plan mode, extended thinking, native subagent generation) obsoleted hand-rolled task-decomposition and roster machinery faster than the roster paid for itself — not a claim that rosters never work, but that *this team's* roster stopped earning its keep relative to what Claude Code's own primitives already did. It is evidence FOR the "rosters are often premature scaffolding" position, but it's a single vendor's self-report of their own reasoning, not an independent before/after study.

---

## 3. PrimeIntellect-ai/prime-agent

An existing brief (`docs/08-agents_work/2026-08-09-prime-agent-research-brief.md` in this repo) already covers this system. Per instructions I treated it as a claim to check, not a summary to trust — see Target #3 below for the verdict. Findings here fold in what that brief got right, since re-deriving verified-correct claims from scratch would be wasted motion; corrections are called out explicitly.

**1. ORCHESTRATION.** Single coding-agent process (`packages/coding-agent`) driving one model at a time via `AgentSession`; sub-agent delegation is model-emitted function calls inside a persistent IPython kernel (RLM), not a coded dispatcher choosing how much machinery a task gets — confirmed, no deterministic routing/tiering layer found in `packages/coding-agent/src/core/*.ts`. The one place a *scope* decision is recorded is the four-limit `AutonomousRuntimeState` (below) — every autonomous run's budget is explicit config, not implicit.

**2. ENFORCEMENT — real, deterministic, and stronger than the brief credited.** `packages/coding-agent/src/core/autonomous.ts` (VERIFIED, read in full, 594 lines): `DEFAULT_AUTONOMOUS_LIMITS = { maxContinuations: 3, maxTurns: 12, maxTokens: 80_000, timeoutMs: 30*60*1000 }` (lines 48-55) and `autonomousLimitReason()` (lines 254-271) checks continuations → turns → tokens → wall-clock in exactly that order, returning a named reason — brief's claim confirmed precisely. `AgentSession._shouldStopAfterTurn` (`agent-session.ts:2165-2199`, VERIFIED) chains terminal-message check → goal-budget accounting (queues a `budget_limit` steer message rather than hard-killing, confirmed) → serialized-refine checkpoint → threshold-compaction check → pending-steering-stop, matching the brief. **What the brief missed:** `autonomous.ts` also implements `runAutonomousQualityGates()` (lines 284-348) — configurable shell commands (`state.gates.commands`) run as a hard precondition before the autonomous loop is allowed to report "not_needed" (i.e. done); each gate failure is captured with stdout/stderr, retried up to `gates.maxRetries` (default 3), and a git-worktree-snapshot hash (`captureGitWorktreeSnapshot`, lines 374-423, SHA256 over untracked files + `git diff`) detects when the agent claims to be done without having changed anything since the last failure, escalating to `retry_exhausted` rather than looping forever on a no-op. This is a genuine deterministic test-runner gate wired into the stop condition — it directly contradicts the brief's "DO NOT ADOPT — Verification posture" claim that "no deterministic self-verification subsystem is documented... no test-runner gate." See Target #3.

**3. CAPABILITY DECLARATION.** MCP integrations are the real instance of this: `packages/coding-agent/docs/mcp-integrations.md` (VERIFIED) confirms built-ins (Linear, Notion) "ship **disabled**" and a call against an unauthenticated integration raises a typed `NotEnabled` error (grepped at lines 33, 70, 201, 208, 222 — VERIFIED) — checked at call time, not decorative. Beyond MCP, tool/skill capability is less formal: `packages/coding-agent/skills/` ships 13 built-in skills (agent-message, agent-observe, attach-image, compact, edit, goal, linear, notion, prime-intellect, refine, rlm-heartbeat, skill-creator, websearch — VERIFIED via `ls`), and `extensions.md` documents 3 overridable built-in tools (`ipython`, `bash`, `edit`) — the brief's noted "tool-surface contradiction" (blog claims IPython-only vs. `extensions.md` listing three) is confirmed still open; I did not find code that resolves it either way in the files read.

**4. MEMORY/STATE.** Two clearly separate mechanisms, contra the brief's "memory M... undocumented" open question — partially resolved: (a) session state via `session-manager.ts` (message history, goals, autonomous counters — all process-local, reset per session unless explicitly persisted); (b) `/refine` — the CRUD-your-own-harness mechanism — goes through a **review → plan → apply pipeline**, not a blind write: `agent-session.ts` around the serialized-refine checkpoint (lines 2212-2234, VERIFIED) names three distinct internal calls, `_reviewAutoRefine`, `_planRefine`, `_applyRefine`, run in sequence with "proper in-flight guards and counter resets." I did not have budget to read those three functions' bodies, so I can only confirm the pipeline *shape* exists (review-then-plan-then-apply, not write-then-done) — whether the review step is a real gate or a rubber stamp remains genuinely open, same as the brief flagged. No resolver anywhere checks a memory/refine claim against filesystem reality that I found (distinct from the *quality gates*, which check task-completion claims, not memory claims specifically).

**5. PROPAGATION/INSTALL.** Not deeply examined (out of scope vs. the manifest question, which belongs to BMAD) — `install.sh` and `prime-agent.sh` exist at repo root but were not read.

**6. OBSERVABILITY — telemetry exists but is write-only in the sense that matters.** `packages/coding-agent/src/core/telemetry.ts` (797 lines, VERIFIED header + write path) batches events (`agent started`, `agent command used`, `agent run completed`, `agent session ended`) and POSTs them to `https://api.primeintellect.ai/api/v1/agent-analytics/events` (a Prime Intellect-owned vendor endpoint), backed by a local `telemetry.json` state file for the installation ID and retry batching. `grep` for any local reader of this stream (`readTelemetry|analyzeTelemetry|loadTelemetry`) inside `packages/coding-agent/src` returns zero hits (VERIFIED) — nothing in the agent's own runtime reads its telemetry back to change behavior. This is vendor product analytics, not agent self-observability; per this study's own rule, write-only counts as no.

**7. STEAL / REFUSE.** Steal: the git-worktree-snapshot stall detector inside `runAutonomousQualityGates()` (`autonomous.ts:294-311`) — hashing untracked files + `git diff` to detect "the agent claims done/retried but nothing actually changed" and escalating to exhaustion instead of looping — a cheap, concrete pattern for catching an autonomous loop that's spinning without progress. Refuse: `AgentExecutionMode`'s "no human input available" continuation prompt (`DEFAULT_AUTONOMOUS_CONTINUATION_PROMPT`, `autonomous.ts:45-46`) instructs the model to "make a reasonable assumption and verify it" when blocked rather than pausing for a human — combined with the open bug the brief already flagged (#986, goal mode re-scheduling itself with no new information), this is a live, acknowledged failure mode: an autonomy design that defaults to guessing over stopping.

### Target #3 — checking the existing brief against the implementation

**Brief is RIGHT on:** (a) the 4-limit autonomous budget, exact numbers and check order (`autonomous.ts:48-55, 254-271`) — precise match; (b) the 4-stage stop-hook composition in `_shouldStopAfterTurn` — precise match including the steer-don't-kill framing; (c) deterministic compaction thresholds (`DEFAULT_COMPACTION_SETTINGS = { reserveTokens: 16384, keepRecentTokens: 20000 }` in `compaction/compaction.ts`, confirmed via grep at lines 128-131, VERIFIED) and the "never cut inside a tool-call/tool-result pair" framing (consistent with the branch-summarization file structure, not read line-by-line); (d) MCP built-ins ship disabled, `NotEnabled` typed error, `bearerTokenEnvVar` static-token path — all confirmed verbatim in `mcp-integrations.md`.

**Brief is WRONG on:** the repo-lineage claim — *"The core turn loop is not even in this repo — packages/coding-agent depends on @earendil-works/pi-agent, and runLoop lives in earendil-works/pi."* This is false as stated. `runLoop` is defined at `packages/agent/src/agent-loop.ts:307` **inside this exact repo** (VERIFIED via grep + read). The confusion is real but subtler than the brief implies: prime-agent is an npm workspace monorepo (`"workspaces": ["packages/*", ...]` in root `package.json`, VERIFIED) whose own local packages are still named `@earendil-works/pi-agent-core`, `@earendil-works/pi-ai`, `@earendil-works/pi-tui`, `@earendil-works/pi-coding-agent` (VERIFIED — every `packages/*/package.json` name field checked) — i.e. the fork kept its upstream npm scope/names rather than rebranding to `@primeintellect/*`. `coding-agent/package.json` lists `"@earendil-works/pi-agent-core": "^0.7.1"` as a "dependency," but under npm workspace resolution that symlinks to the *local* `packages/agent` directory, not a fetch from the separate `earendil-works/pi` GitHub repo. Comparing the two repos directly (both shallow git clones, so no commit history to diff): `earendil-works/pi`'s own `packages/agent/package.json` is at version **0.84.1** (VERIFIED) versus prime-agent's frozen **0.7.1** across all four local packages (VERIFIED) — consistent with "prime-agent forked pi at 0.7.1 and has not resynced since," and pi itself has since grown to 10 packages (adding evals, session-backends, server, protocol, telemetry, client) that prime-agent never picked up. So: "productized, heavily extended superset of pi" (the brief's framing) is directionally right, but "the loop is not in this repo" is factually wrong — it's a stale, locally-vendored fork of the loop, still living under its original package identity.

**Brief is WRONG/overstated on:** "no deterministic self-verification subsystem is documented... no test-runner gate" (the load-bearing claim behind "DO NOT ADOPT — Verification posture"). `autonomous.ts`'s `gates.commands` + `runAutonomousQualityGates()` mechanism (detailed under Q2 above) is precisely a deterministic test-runner gate wired into the autonomous stop condition, with retry-exhaustion and stall detection. The brief itself flagged this claim as "an answer from absence... not exhaustively grepped," which was the right caveat — a targeted read of `autonomous.ts` (which the brief's own citations already pointed at, for the budget claims) falsifies it. This matters for the brief's bottom line: "on verification, Beamix's qa.js is already ahead" may still be true in *sophistication* (LLM-judge + adversarial reviewer vs. prime-agent's shell-command gate), but the premise that prime-agent has *no* deterministic gate at all is incorrect — it has one, just a shallower one (pass/fail shell commands, not a judged verdict).

**Brief's open question #1 ("/refine write gating is unknown") — partially resolved, not fully.** The serialized-refine checkpoint code (`agent-session.ts` near lines 2212-2234, VERIFIED) confirms a **review → plan → apply** pipeline exists (`_reviewAutoRefine`, `_planRefine`, `_applyRefine` as three distinct guarded phases) — so it is not a blind, ungated write as the phrasing of the open question might suggest to a reader. Whether that review step is a meaningful gate (gets to say no) or a formality was not established — the function bodies were not read within this study's budget. Flag this as the most valuable next-read for anyone actually adopting `/refine`-style self-modification.

**Not independently re-checked (taken from the brief on trust, time did not permit re-verification):** the "no RL in the harness" launch-post quote, the day-one-defect issue numbers (#1005, #995, #648, #999, #1008, #986, #1011 — these are GitHub Issues, not in the local clone, so unverifiable from disk), the ARC-AGI-3/long-context benchmark numbers, and the sandboxing claim (`architecture.md`/`rlm-runtime.md` quotes) — these read as internally consistent with what I saw of the codebase (process-based worker/kernel separation, no container/VM references encountered) but I did not re-open those specific doc files.

---

## 4. earendil-works/pi

This is the upstream harness prime-agent forked (see Target #3): prime-agent's `packages/{agent,ai,tui,coding-agent}` are a frozen fork of pi's same-named packages, still published under the `@earendil-works/*` npm scope, pinned at pi's v0.7.1 (VERIFIED via `package.json` version comparison, see prime-agent section) while pi itself has moved to v0.84.1 and grown from 4 to 10 packages (VERIFIED — `find packages -maxdepth 1 -type d` lists `evals, session-backends, coding-agent, tui, server, agent, protocol, ai, telemetry, client`; prime-agent has only 4 of these). Findings below focus on what's genuinely different from prime-agent, since the shared core (`agent-loop.ts`/`runLoop`, the general shape of `AgentSession`) was already covered there.

**1. ORCHESTRATION.** Same shape as prime-agent at the core-loop level (model-driven sub-agent delegation, no coded dispatcher tiering tasks). One documented difference worth flagging: `AGENTS.md` (VERIFIED, read in full) explicitly plans for **multiple concurrent pi sessions sharing one working directory** — "Multiple pi sessions may be running in this cwd at the same time, each modifying different files. Git operations that touch unstaged, staged, or untracked files outside your own changes will stomp on other sessions' work." The mitigation is pure convention, not code enforcement: "Stage explicit paths... never `git add -A`/`git add .`," "if a conflict is in a file you did not modify, abort and ask the user." This is a real, named multi-agent-collision hazard with a documented-but-unenforced discipline as the only guard — no lock file, no coordination protocol, no code that detects a session touching another's files.

**2. ENFORCEMENT — the strongest of the four repos, and the only one with a live git hook.** `.husky/pre-commit` (VERIFIED, read in full) is a real, registered hook — installed automatically via `"prepare": "husky"` in root `package.json` (VERIFIED), which npm runs on every `npm install`. It actually blocks a commit if: (a) `node scripts/check-lockfile-commit.mjs` fails (matches `AGENTS.md`'s stated rule: "Pre-commit blocks lockfile commits unless `PI_ALLOW_LOCKFILE_CHANGE=1`" — VERIFIED, that env-var escape hatch is documented, not found in the hook script itself but consistent with it); (b) `npm run check` (lint + format + typecheck) fails; (c) for changes touching `packages/ai/*`, `packages/web-ui/*`, `package.json`, or `package-lock.json`, a browser-smoke check fails. This is categorically different from BMAD and agent-os (zero hooks, CI-only or pure-prose enforcement) — pi has a live, local, un-bypassable-by-default gate at commit time. It is still developer/repo-hygiene enforcement, not agent-runtime tool-call enforcement (no PreToolUse/PostToolUse equivalent found).

**3. CAPABILITY DECLARATION.** README states plainly (VERIFIED): "Pi does not include a built-in permission system for restricting filesystem, process, network, or credential access. By default, it runs with the permissions of the user and process that launched it." Sandboxing is explicitly pushed out to the operator via three documented external patterns (`packages/coding-agent/docs/containerization.md`, not read in full but referenced in README): a Gondolin micro-VM extension, plain Docker, or "OpenShell" policy-controlled sandbox. This is candid and consistent with what the prime-agent brief found downstream ("Workers and kernels are separate processes... not security sandboxes") — confirms that claim traces to pi's own documented design choice, not something prime-agent introduced or hid. No MCP-specific integration docs (`mcp-integrations.md`) were found in pi itself (`find . -iname "*mcp*.md"` under `packages/coding-agent/docs` returns nothing, VERIFIED) — the Linear/Notion/Prime-Intellect-specific MCP capability-gating (`NotEnabled` typed errors, disabled-by-default built-ins) documented in the prime-agent section appears to be a prime-agent-specific addition on top of base pi, not inherited from it.

**4. MEMORY/STATE — more built out than prime-agent's.** pi ships a dedicated `packages/session-backends` package with (at minimum) a `sqlite-node` backend (VERIFIED via directory listing — `packages/session-backends/sqlite-node`), i.e. persistent, structured session storage as a first-class pluggable concern; prime-agent has no equivalent package at all (confirmed absent from its 4-package roster). pi also ships `packages/telemetry` (`@earendil-works/pi-telemetry`, VERIFIED via README) as a proper vendor-neutral span/context contract (spans, attributes, events, status, parent/child — OpenTelemetry-shaped) with an in-memory reference adapter and explicit statement of what it does NOT do: "no exporter, global current-span state, or dependency on a telemetry backend" (VERIFIED, package README). Unlike a purely decorative contract, it IS wired into the actual coding-agent: `grep` for `pi-telemetry`/telemetry imports inside `packages/coding-agent/src` (excluding tests) hits two live call sites, `core/provider-attribution.ts` and `modes/interactive/interactive-mode.ts` (VERIFIED). I did not have budget to confirm whether anything reads spans back to alter agent decisions (vs. purely exporting/displaying them), so "does a resolver check a memory/telemetry claim against reality" remains open for pi specifically, same caveat as prime-agent's `/refine` question.

**5. PROPAGATION/INSTALL.** Ships as an npm package (`@earendil-works/pi-coding-agent`) and as standalone binaries built via `scripts/build-binaries.sh`, with a documented supply-chain-hardening stance in the README: "We treat npm dependency changes as reviewed code changes... Direct external dependencies are pinned to exact versions." Release source archives ship a `SHA256SUMS` file (VERIFIED, README "Building standalone binaries from release source" section) — this is release-artifact integrity (verifying the tarball you downloaded), not the per-file "did the user customize this and how do we not clobber it" problem BMAD solves; pi has no equivalent to BMAD's `files-manifest.csv` because pi isn't installed *into* a target project's tree the way BMAD/agent-os are — it's a CLI tool installed globally/locally via npm, so there's no analogous "project's own customizations" surface to protect.

**6. OBSERVABILITY.** The `pi-telemetry` package (above) is architecturally the strongest observability primitive of the four repos — real span/event/status modeling, not a flat log — but ships with an explicit no-exporter, bring-your-own-backend design; without an application wiring an adapter (OpenTelemetry, Sentry, or otherwise), spans go nowhere durable. Two live call sites inside coding-agent confirm it's used, but full read-side verification (is there a default local exporter a user can inspect, e.g. a debug view in the TUI) was not completed within budget.

**7. STEAL / REFUSE.** Steal: the live `.husky/pre-commit` gate (`.husky/pre-commit`) — a real, auto-installed, un-bypassable-by-default block on lint/type/format failures and unreviewed lockfile changes, which is the one genuinely enforced (not merely documented) mechanism found across all four repos at the repository-hygiene layer; pair it with the `PI_ALLOW_LOCKFILE_CHANGE=1` explicit-override pattern (name the escape hatch, require an explicit flag to use it) as a template for "block by default, allow an intentional override." Refuse: shipping zero built-in filesystem/process/network permission system by design (README, verbatim) while offloading all containment to the operator via optional, separately-documented external sandboxes — reasonable for a personal CLI tool used by one trusted developer, but a footgun if any part of Beamix's own agent system were to adopt pi's core loop without independently re-adding the sandboxing pi explicitly does not provide.

---

## Cross-repo comparison

| Question | BMAD-METHOD | agent-os v3 | prime-agent | pi |
|---|---|---|---|---|
| Orchestration | Personas as SKILL.md; only party-mode spawns real subagents, mode chosen but not recorded | None — zero agents shipped by design (see Target #2) | Model-driven sub-agent calls in an IPython kernel; no coded tiering; multi-limit budget is the one explicit scope decision | Same core loop as prime-agent; additionally documents (not enforces) multi-session collision risk in one cwd |
| Enforcement | Zero runtime hooks; CI-only skill linter (`validate-skills.js` + LLM pass) | Zero hooks of any kind; one whole-folder confirm prompt at install | Real: deterministic autonomy-budget stop + shell-command quality gates with stall detection (`autonomous.ts`) | Real: live `.husky/pre-commit` blocking lint/type/format + reviewed lockfile changes — strongest of the four |
| Capability declaration | Does not exist (0 hits for `allowed-tools`) | Does not exist | Real for MCP only (`NotEnabled`, disabled-by-default built-ins); tool-surface docs self-contradict (IPython-only vs. 3 built-ins) | No built-in permission system at all, by explicit design; sandboxing pushed to 3 external patterns |
| Memory/state | `memlog.py`: append-only, write-only, no resolver checks it | `index.yml`: description cache, no resolver | Session state process-local; `/refine` has a review→plan→apply pipeline (depth unverified) | `session-backends` package (SQLite) — more built-out persistence than prime-agent has at all |
| Propagation/install | Per-file SHA256 diff (`files-manifest.csv`); modified files → `.bak` sibling, new version wins live; custom files fully restored (Target #1) | Whole-folder `cp`, one all-or-nothing confirm prompt, no per-file protection | Not examined | Release-tarball `SHA256SUMS` (artifact integrity, not per-file project-customization protection — different problem) |
| Observability | None found | None found | `telemetry.ts`: real but vendor-only, write-only POST to Prime Intellect's own endpoint, nothing local reads it back | `pi-telemetry`: proper span/event contract, wired into 2 real call sites, no exporter by design — best-architected but unverified whether anything reads it back |
| Notable finding | Installer's SHA256 diff is the one real piece of enforced code in the repo | Twice-repeated, explicit "our own roster stopped earning its keep" rationale (v2.1.0 then v3.0) | Brief's "no test-runner gate" claim is falsified by `autonomous.ts`'s shell-command quality gates; "loop isn't in this repo" claim is also false | Only repo of the four with a live, un-bypassable-by-default commit-time gate |

## Headline claims

1. **BMAD's per-file installer manifest is real and does what's claimed, with one important nuance: it detects but does not preserve edits.** `files-manifest.csv` (SHA256 per file, regenerated every install) lets `detectCustomFiles()` (`tools/installer/core/installer.js:844-955`) tell a net-new user file from a user-edited shipped file before any overwrite happens. Net-new files are fully restored verbatim; edited shipped files are NOT restored to their live path — the new shipped version wins there, and the user's prior edit survives only as a sibling `.bak` file for manual reconciliation.
2. **buildermethods/agent-os v3 didn't shrink its agent roster — it deleted it twice, on the record, and said why both times.** v2.1.0 (Oct 2025) killed a `roles/implementers.yml`/`verifiers.yml` system because it added "no real benefit over simply using available tooling... for spinning up your subagents"; v3.0 (Jan 2026) then retired orchestration/implementation entirely because "frontier models handle this well on their own now." Current `main` has 22 files and zero agent files of any kind — verified by direct listing, not inference.
3. **The existing prime-agent brief got the mechanism details right but two of its highest-stakes framing claims wrong, in the direction of the framework's favor.** It correctly nailed the exact autonomy-budget numbers, check order, and stop-hook chain — but its claim that "the core turn loop is not even in this repo" is false (`runLoop` lives in `packages/agent/src/agent-loop.ts` inside this exact repo; the confusion is that the local workspace package kept its upstream `@earendil-works` npm name after the fork), and its "DO NOT ADOPT — no deterministic self-verification subsystem" claim is falsified by `autonomous.ts`'s own shell-command quality-gate system with retry-exhaustion and git-snapshot stall detection — a real, if shallow, test-runner gate the brief said didn't exist.

## What I did not get to

- **prime-agent PROPAGATION/INSTALL (Q5)** — `install.sh` and `prime-agent.sh` at repo root were not opened. Given BMAD's manifest mechanism was the priority target and consumed significant budget, this was deprioritized.
- **prime-agent `/refine` review-gate depth (open question carried over from the brief)** — confirmed the pipeline shape (`_reviewAutoRefine` → `_planRefine` → `_applyRefine` as three distinct guarded phases in `agent-session.ts`) but did not read the function bodies to determine whether the review step can actually reject a change or is a formality. This is the single highest-value next read for anyone adopting `/refine`-style self-modification, per the original brief's own flag.
- **pi's `containerization.md` and `mcp` posture beyond the README summary** — referenced but not opened in full; the three sandboxing patterns (Gondolin, Docker, OpenShell) are named, not evaluated.
- **pi telemetry read-side** — confirmed two live write call sites (`provider-attribution.ts`, `interactive-mode.ts`); did not confirm whether any exporter/adapter is actually wired by default such that a user or the agent itself can read spans back (vs. the package existing purely as a write contract with no consumer configured).
- **GitHub-hosted evidence for either repo** (prime-agent's cited issue numbers #1005/#995/#648/#999/#1008/#986/#1011; agent-os's actual GitHub issue #328/#327 referenced in its Unreleased changelog section) — none of this is in the local shallow clones, so none of it was independently re-verified; taken on trust from the brief / changelog text respectively.
- **BMAD's `bmad-agent-*` persona SKILL.md bodies** (PM, UX-designer, analyst, architect, dev) were sampled (frontmatter + activation section of `bmad-agent-dev` only), not read end-to-end; the ORCHESTRATION finding for BMAD rests mainly on `party-mode`, which is the one place real multi-agent spawning happens — the single-persona skills' internal workflow logic (step-by-step files under `src/bmm-skills/plan/*`) was not audited.

All security note: no file, skill, or prompt content from any of the four repos was copied into this repository — only short verbatim quotes (each attributed to its source file) appear above, consistent with the read-only/no-import instruction. No instruction-like text encountered inside the studied repos was followed; nothing suspicious of that kind was found.
